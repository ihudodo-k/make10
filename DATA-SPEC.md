# Make10 データ生成仕様書

## 1. これは何か

スマホ用パズルゲーム「Make10」のための、問題データ生成パイプライン。

ゲームのルール:
- 4つの数字（各 0〜9）が画面に**固定の順序で**並ぶ
- プレイヤーは数字と数字の間に演算子を配置して、計算結果を 10 にする
- 使える演算子: `+` `-` `*` `/` `^`（累乗）`!`（階乗）`(` `)`

このプロジェクトの成果物は、ゲーム本体が読み込む問題データ（JSON）である。

## 2. 絶対に守る仕様

以下は決定済み。実装時に勝手に解釈を変えないこと。

| 項目 | 決定 |
|---|---|
| 数字の順序 | ProblemID の並び順で固定。入れ替え不可 |
| `^` の結合 | **右結合**（`a^b^c` は `a^(b^c)`）。数学の標準に従う |
| `0 ^ 0` | `1` とする |
| 指数 | 整数のみ。非整数の指数は無効 |
| `0` の負冪 | 無効（`0^-1` は解として認めない） |
| 階乗の引数 | 非負整数のみ。負数・分数への階乗は無効 |
| ゼロ除算 | 無効 |
| 中間結果への階乗 | 許可（`( 1 + 4 - 5 )!` のような形は有効） |
| 数値計算 | **必ず有理数（分数）で厳密に行う。浮動小数点は使用禁止** |

### 階乗の表記（重要）

このゲームで使うのは**単項の階乗を繰り返し適用する**操作であり、二重階乗ではない。
両者は別の演算なので、表記で区別しなければならない。

```
(3!)!  = 6!  = 720      ← このゲームで使うのはこちら（階乗を2回適用）
3!!    = 3 x 1 = 3      ← 数学の標準記法における二重階乗。このゲームでは使わない
```

したがって:

- **`display` に `!!` という並びを出してはいけない。** 階乗の子が階乗のときは必ず括弧で囲み、
  `( 3! )!` と出力する
- **`verify` のパーサは、連続する `!`（括弧を挟まない `!!`）を構文エラーとして拒否する。**
  これは再発検出のための安全網であり、パーサを甘くしてはいけない
- ゲーム本体の入力UIでも `!!` は入力不可にする。プレイヤーは `( 3! )!` と組み立てる

過去に `^` の結合順で同じ失敗をしている（木は正しいのに文字列の読み方が標準とずれていた）。
**表示用文字列は、標準的な記法で読んだときに木と同じ意味になること**が必須要件である。

### 計算量の安全ガード

これらは「面白くないから除外する」ためのものではなく、**計算の爆発を防ぐため**の上限。
1箇所の定数にまとめ、あとから変更できるようにする。

```python
MAX_FACTORIAL_ARG = 12   # 階乗の引数の上限
MAX_EXPONENT_ABS  = 24   # 指数の絶対値の上限
```

### この値の根拠（旧データ 534,163 件の実測）

大きな階乗・指数は「打ち消し合い」で有効に使われることがあるため、安易に小さくしてはいけない。
実際に有効な解が存在する:

```
0! + 0! + 8! / 7!            = 1 + 1 + 8  = 10   （階乗の引数 8）
( ( 0! + 0! ) * 5 )! / 9!    = 10! / 9!   = 10   （階乗の引数 10）
5! - ( 2 + 9 )! / 9!         = 120 - 110  = 10   （階乗の引数 11）
( 0 - 0! ) ^ 4! + 9          = (-1)^24 + 9 = 10  （指数 24、底が -1 なので偶奇が効く）
```

一方、感度テスト（その階乗・指数の値だけをずらして結果が変わるか調べる）の結果:

- 階乗の引数が11以上の解 2,482件のうち、**結果に影響しているものは 0 件**
  （2,410件は `( 0 * ( 7! / 5! )! )! + 9` のように 0 を掛けて無効化されている。
    残り72件は `^` の結合順バグ由来で、修正すれば消える）
- 指数が9を超えて意味があるのは、上記の `(-1)^偶数` 系統のみ

上限を 12 / 24 にしても、**9,161問すべてで解が1つ以上残る**ことを確認済み。

未探索の領域が1つある: 旧生成器は指数を100で打ち切っていたため、`( 0 - 0! ) ^ 5!`（指数120）
のような解は探索されていない。上限を上げれば新規に見つかるが、同じ「±1の偶数乗」トリックの
変種なので優先度は低い。

## 3. 過去に起きたバグ（同じ失敗を繰り返さないこと）

### 累乗の結合順が壊れていた

旧実装は RPN（逆ポーランド記法）で正しく計算していたが、**それを人間が読む式に変換するときに
括弧を落としていた**。`(0^0)^0` という木構造が `0 ^ 0 ^ 0` と出力され、標準の右結合で読むと
別の式になっていた。194,445件中 470件が誤り。

この教訓から:

- **数式の正準表現は RPN のトークン列とする。表示用の文字列はそこから毎回生成する**
- 文字列を計算やグループ化のキーに使わない
- `verify` サブコマンドで、**表示用文字列を独立実装のパーサで再評価**し、
  全件が 10 になることを確認する。RPN を再評価するのでは意味がない（表示側のバグを見逃す）

### 掛け算の右に割り算が来ると括弧が落ちていた

_render の右オペランドの括弧条件が「同順位かつ - か /」だったため、
op が * で右の子が / のときに括弧が落ちていた。
4 * ( 9 / 4 ) が「4 * 9 / 4」と出力され、標準の左結合で読み直すと
( 4 * 9 ) / 4 という別の木になる。値は一致するため値ベースの検証では
検出できず、3,433 件の解が分数ボーナス +5 を過剰に得ていた。
出題 797 問の難易度が誤り、うち 123 問が誤って挑戦モードに入っていた。

対策として verify に往復チェック（display を独立パーサで読み直し、
shape から復元した木と構造比較する）を追加した。

### 並列処理でヘッダー行が行方不明になった

旧実装は `ConcurrentBag` にヘッダーとデータを混ぜて入れており、出力CSVのヘッダーが
最終行に出ていた。並列処理の結果を集める入れ物に、順序が必要なものを混ぜないこと。

### 中間ファイルが12個に増えて管理不能になった

CSV を段階ごとに吐き出した結果、3世代のデータが混在して収拾がつかなくなった。
**中間データは SQLite 1ファイルに集約する。CSV は人間が確認するときだけ吐く。**

## 4. データモデル

`make10.db`（SQLite）1ファイル。

```sql
CREATE TABLE solutions (
    id             INTEGER PRIMARY KEY,
    problem_id     TEXT    NOT NULL,   -- "0004"
    shape          TEXT    NOT NULL,   -- スロット記号のRPN: "n0 ! n1 ! + n2 ! + ! n3 +"
    display        TEXT    NOT NULL,   -- shape から生成した表示用: "( 0! + 0! + 0! )! + 4"
    cnt_add        INTEGER NOT NULL,
    cnt_sub        INTEGER NOT NULL,
    cnt_mul        INTEGER NOT NULL,
    cnt_div        INTEGER NOT NULL,
    cnt_pow        INTEGER NOT NULL,
    cnt_fac        INTEGER NOT NULL,
    cnt_paren      INTEGER NOT NULL,
    max_fac_arg    INTEGER NOT NULL,   -- この式で使われた階乗の引数の最大値
    max_exp_abs    INTEGER NOT NULL,   -- 指数の絶対値の最大値
    uses_fraction  INTEGER NOT NULL,   -- 途中で分数が現れたか (0/1)
    score          INTEGER,            -- annotate で埋める
    is_redundant   INTEGER DEFAULT 0,  -- curate で埋める
    redundant_why  TEXT,               -- 除外理由
    is_repr        INTEGER DEFAULT 0   -- 代表解として採用したか
);

CREATE TABLE problems (
    problem_id      TEXT PRIMARY KEY,  -- "0004"
    solution_count  INTEGER,           -- 冗長解を除いた解の数
    min_score       INTEGER,
    max_score       INTEGER,
    repr_solution_id INTEGER           -- 代表解
);

-- 制約付き問題（第6-B章）。1つの problem_id から複数の puzzle が生まれる
CREATE TABLE puzzles (
    id                  INTEGER PRIMARY KEY,
    problem_id          TEXT    NOT NULL,
    rules               TEXT    NOT NULL,  -- 正規化した制約: "*=0" / "!>=2 | /=1"
    rule_count          INTEGER NOT NULL,  -- 制約の個数 (0〜2)
    survivor_count      INTEGER NOT NULL,  -- 制約を満たす解の総数（冗長解も含む）
    repr_survivor_count INTEGER NOT NULL,  -- うち is_repr=1 の数
    example_solution_id INTEGER,           -- 解答例（残存解のうち最も読みやすいもの）
    min_score           INTEGER,           -- 判定基準B: 残存解の最小スコア
    base_min_score      INTEGER,           -- 制約なしのときの最小スコア
    harder_by           INTEGER            -- min_score - base_min_score
);

CREATE INDEX idx_sol_problem ON solutions(problem_id);
CREATE INDEX idx_sol_shape   ON solutions(shape);
CREATE INDEX idx_puz_problem ON puzzles(problem_id);
```

### shape について

数字そのものではなく **スロット番号** (`n0` `n1` `n2` `n3`) で RPN を記録する。
こうすると問題をまたいで「同じ形の解」をグループ化できる。

演算子トークン: `+` `-` `*` `/` `^` `!`

例: `( 0! + 0! + 0! )! + 4` → `n0 ! n1 ! + n2 ! + ! n3 +`

## 5. サブコマンド

1本のプログラムに集約する。`Main` が複数あるような構成にはしない。

```
python make10.py generate   # 全探索して solutions に投入
python make10.py annotate   # 特徴量とスコアを付与
python make10.py curate     # 冗長解にフラグを立て、代表解を選出
python make10.py constrain  # 演算子の使用回数による制約付き問題を生成（第6-B章）
python make10.py verify     # 独立パーサで全件を再検証
python make10.py export     # ゲーム用 JSON を出力
python make10.py blob       # docs/index.html の BLOB を組み直す（第8-B章）
python make10.py dump TABLE # 人間確認用に CSV を吐く（Excel用）
```

各サブコマンドは冪等であること。2回実行しても結果が変わらない。

### generate

0000〜9999 の全 10,000 通りについて、RPN を列挙して 10 になるものを収集する。
同一 ProblemID 内で shape が重複するものは 1 件にまとめる。

**この段階では冗長な解も含めて全部保存する。** 何を冗長とみなすかは後から変えたくなるので、
高価な探索をやり直さずに済むようにしておく。

### verify

`solutions.display` の文字列を、generate とは**独立に実装した再帰下降パーサ**で評価し、
全件が厳密に 10 になることを確認する。1件でも合わなければ非ゼロ終了。

これは飾りではなく、このプロジェクトで最も重要なコマンド。

## 6. 冗長解の除外ルール（curate）

### 6-1. 数字を潰している解

**判定の基準（5.2 で作り直した）:**

> その数字を別の値に変えても式の値が変わらないなら、その数字は寄与していない

4 つの数字の位置を順に取り、その位置だけを 0〜9 の他の値に差し替えて評価し、
**有効に評価できた差し替えのすべてで結果が変わらなければ**その数字は潰されていると
判定する（`classify_nullified`）。1 つでも潰れていれば解ごと 6-1 とする。

- **評価できない差し替えは判定から除く**（0 除算、階乗の引数が負・非整数・上限超えなど）。
  基準は式の「値」の話で、「評価できない」は値ではない。これを「値が違う」と数えると、
  0 除算を避けるだけの除数が寄与を主張できてしまう（旧判定の取りこぼしが 26,764 本に膨らんだ）
- 逆に「有効な差し替えが 1 つも無い」桁を「潰されている」と数えるのも誤り。
  `3343` の `( 3! )! / ( 3! * 4 * 3 )` の最初の 3 のように、**差し替えられないだけで
  式の値を決めている**数字を落としてしまう（27,507 本に誤検出が混ざった）。
  よって「判定できない」は寄与している側に倒す
- `0 ^ 0` に例外を設けない。底を何に変えても `x ^ 0 = 1` なので底は寄与していない。
  数字を潰す形しか無い問題は解が全滅するが、6-4 / 6-5 の救済に任せる。実例は `0075`
  （数字 0,0,7,5）で、6 解すべてが `0 * 7` などで 7 を潰すので全部 6-1 に掛かり、
  6-4 が `( 0! + ( 0 * 7 )! ) * 5` を 1 件だけ残す（`make10.db` で 6-4 の救済は 512 問）。
  なお `0009`（数字 0,0,0,9）は全滅しない ―― `0!` で 1 を作れるので
  `0 + 0 + 0! + 9` のような非冗長な解が残る（562 解中 189 解）

**パターンの列挙で補う**（`_nullified_by_pattern`）。桁単位の判定は「差し替えられる桁」に
しか使えないので、定義域が狭い部分式は部分式単位で見るしかない。両方を OR で使う。

- `0 * x` / `x * 0` / `0 / x` / `x ^ 0` / `1 ^ x` / `0 ^ x`
- 例: `3639` の `( 3! )! ^ ( 6 - 3! ) + 9`（`= 720 ^ 0 + 9`）。`X ^ 0` は X が何であれ 1 だが、
  `( d! )!` は d=3 以外すべて無効（`1!`/`2!` は枝刈り、`4! = 24` から `24!` は上限超え）
  なので、桁を差し替える判定では「有効な差し替えが無い」になってしまう。
  実測でこの型の取りこぼしが 2,100 本あった

**なぜパターンの列挙をやめたか。** 5.1 までは `0 * x` / `x * 0` / `x ^ 0` / `1 ^ x` の
4 つを列挙し、しかも「`x` が演算子を 1 つ以上含む場合」に限っていた。そこを 6-2 の
`x * 1` が偶然せき止めていたが、6-2 を廃止すると `1 ^ 6` が露出し、葉に広げると
今度は `0 / x` へ、それも塞げば `0 ^ x` へ、と解答例が未対応の形へ移り続けた。
**パターンを足す限り「次に何が漏れているか」が分からない。**

実データにあった典型例:
```
( 0 * ( 3 * 5 )! )! + 9    ←  実質 0! + 9
```

### 6-2. 恒等演算（5.2 で廃止）

**この判定は 5.2 で使わなくなった**（`classify_identity` は記録として関数だけ残してある）。

理由: 数字 4 つは全部使わなければならないので、0 を消費する最も自然な形が `+ 0` である。
それを「値を変えない」として除外すると、たとえば `0025` の解答例が
`0 + 0 + 2 * 5`（score 4）ではなく `0 - ( 0 - 2 * 5 )`（score 5）になり、
**人間が書かない式が解答例になってしまった**（実測 2,423/9,161 = 26.4% の問題が該当）。

かつて 6-2 が対象にしていたもの（記録）:

- `x + 0`, `x - 0`, `x * 1`, `x / 1`, `x ^ 1`
- 値が 1 または 2 のものへの階乗（`1! = 1`, `2! = 2` で変化しない）

うち **`1!` と `2!` は生成の段階で枝刈りしている**（`build_evaluator`）ので、
6-2 の廃止後も解には出てこない。したがって階乗の引数は **0 か 3 以上**しかない。
`0! = 1` は値が変わるので、もとより恒等演算ではない。

### 6-3. 実質的に同じ解の圧縮

6-1 を通過した解を、以下のキーでグループ化して 1 つに絞る。

```
(problem_id, cnt_add + cnt_sub, cnt_mul + cnt_div, cnt_pow, cnt_fac, 括弧構造)
```

代表解の選び方（プレイヤーにとって読みやすい順）:
1. 括弧が少ない
2. 割り算が少ない
3. 引き算が少ない
4. 表示文字列が短い

**5.2 から 6-3 は `is_redundant` を立てない。** 痕跡（`redundant_why` に
`6-3: compressed into id=N`）だけ残す。`is_repr`（一覧に出す代表）を選ぶのが 6-3 の役目で、
解答例と `min_score` の母集団は「6-1 を除いた全解」である。

### 6-4. 問題を空にしてはならない（6-1 より優先）

このゲームは**4つの数字をすべて使わなければならない**。そのため、余った数字を吸収する
ための `+ 0` や `* 1` は「無駄な演算」ではなく**必須の演算**であることがある。

したがって:

- 6-1 は、**同じ problem_id に生き残りが1つ以上あるときだけ**適用する
- ある問題の解が全滅する場合は、その中から 1 件を残し、`redundant_why` に
  `6-4: kept (only solution)` と記録する
- 結果として、**解が1つ以上ある9,161問すべてが `problems` テーブルに残る**こと

**残す 1 件は「スコア最小 → 読みやすさ → id」で選ぶ**（`_rescue_sort_key`。5.2）。
救済されるのは「その問題の解が全部 数字を潰す形」という問題で、どうせ潰す形しか
無いなら、その中で最も簡単なものを見せるべきである。5.2 の途中までは読みやすさだけで
選んでいたため、439 件中 171 件で最小スコアでない解を救済していた（例: `9949` は
`( 9 - 9 )! ^ 4 + 9`（15）を採用し、最小の `( 9 / 9 ) ^ 4 + 9`（10）を落としていた）。
救済解はそのまま解答例になり `min_score` にもなるので、難易度が過大に付き、
ヒントも余計に難しい式を見せていた。**6-5 / 6-6 と基準を揃えてある。**

言い換えると、6-1 は「もっと良い解があるから捨てる」ルールであって、
「この形は絶対にダメ」というルールではない。

### 6-5. 制約つき puzzle も空にしてはならない（constrain、5.2）

6-4 は problem 単位の救済だが、**puzzle 単位でも同じことが起きる。**
制約を満たす解が「数字を潰す形」しか無い puzzle がある（例: `0158` の
`0! + 1 ^ 5 + 8`）。`is_redundant` は「どれを解答例にするか」「一覧に何本並べるか」を
決める印であって、パズルの存在を左右するものではない。上の式は合法な入力で
正解になるので**プレイヤーは解ける。解けるパズルを帳簿の都合で消さない。**

- 制約を満たす解に冗長でないものが 1 つも無ければ、制約を満たす全解から解答例を選ぶ
- `is_redundant` は下げない（その解は確かに数字を潰している）。
  `redundant_why` の末尾に `| 6-5: kept as constrained puzzle example` を足す
- 印を付けるのは**採用された puzzle だけ**。`harder_by` で落ちた候補に付けると
  「救済されたのに存在しない puzzle」の痕跡が残ってしまう

### 6-6. 解答例は必ず一覧に載っている（constrain、5.2）

`is_repr` は 6-3 が problem 単位で選んだ代表なので、制約つき puzzle では
「制約を満たす最良の解」が代表に選ばれていないことがある。そのままだと
**ヒントが指す式が一覧に無い**（ヒント 1・2 から一覧へ辿れない）。

- 解答例に選ばれた解は強制的に `is_repr = 1` にする
- 「解答例は必ず一覧にある」を DB 側の不変条件にする
- `repr_survivor_count` はこの昇格を織り込んでから確定させる
- **`curate` → `constrain` の順に流すこと。** `constrain` だけを 2 度流すと
  前回の昇格が残る（`curate` が `is_repr` を 0 に戻してから代表を付け直す）

## 6-B. 制約付き問題の生成（constrain）

数字の組み合わせは 9,161 通りしかない。そこに「演算子の使用回数」の制約を加えることで、
同じ数字でも別のパズルとして成立させ、問題数を増やす。

例: ProblemID 6988 は無制約だと解が8つあるが、「`*` を使用禁止」とすると3つに絞られる。

```
6! / ( 9! / ( 8! / 8 ) )
6! / 9! / ( 8 / 8! )
6! / ( 9! / 8! ) / 8
```

3つとも同じ計算を括弧の位置だけ変えたもので、プレイヤーから見れば1つの発想。
「掛け算を使わずに10を作れ」という良問になる。

### 制約の種類

**演算子の使用回数のみ**を使う。スロット固定や括弧構造は使わない
（プレイヤーに提示しづらく、パズルとして理解できないため）。

**制約は1問につき1個だけ。形は「使用禁止」のみ。**

```python
CONSTRAINT_RULE_KINDS = ("=0",)   # 使用禁止のみ
MAX_CONSTRAINTS = 1
```

- `op = 0` … その演算子を使えない（「×は使えません」）

対象は `+` `-` `*` `/` `^` `!` の6種類なので、候補は最大6通り。

`op = 1`（ちょうど1回使う）は**採用しない**。実際に遊んで判断した結果である。

- `=0` は「この道は通れない」という**選択肢を狭める**制約で、パズルとして自然
- `=1` は「これを必ず使え」という**手順の指示**になり、解き方を押し付けられる感覚が出る

パズルは自分で道を見つけるものなので、後者は体験を損なう。

`=1` には「特定の演算子を必ず使わせる教材」としての役割もあったが、それは
**その演算子を使わないと解けない問題**を選ぶことで代替できる（第6-D章）。
画面にルールを表示しなくても、プレイヤーは行き詰まって自分で気づく。

この判断で puzzle は 31,525 → 18,252 件になる。挑戦モード（難易度17以上）は
2,126 → 1,142 件に減るが、1日3問解いても1年持つ量なので問題にしない。

`>=` の形式と、制約を2個組み合わせる形は**使わない**。実測で比較したところ、
制約付き puzzle が 482,268 件から 73,475 件に減る一方、カバーできる問題数は
9,161 → 8,490 とわずかしか減らなかった。落ちるのは「読みにくいうえに絞り込みも弱い」
制約ばかりで、捨てるコストがない。

候補が12通りしかないため、1問あたりの puzzle 数が構造的に上限12で頭打ちになる。
これが最大の利点で、後段で選抜する工程が不要になる。

`op = 1` を「1回まで（0回でもよい）」にしないのは、プレイヤーが0回で解いた瞬間に
使用禁止版と同じ問題になってしまい、パズルとして成立しないため。

### 探索の手順

旧C#実装は「解ごとに制約を全探索」する構造で非常に重かった。
**問題ごとに候補を列挙して残存解を見る**ほうが自然で、桁違いに速い。

1. problem_id ごとに、その問題の**全解**（`is_redundant` に関係なく全部）を取得する
2. 候補となる制約ルールを列挙する（6演算子 × 2形式 = 最大12通り）
3. 各ルールについて「そのルールを満たす解」をビットマスクで表す
4. 残存解が空でなく、かつ無制約より真に少ないものを puzzle 候補にする
5. 採用条件（後述）を満たすものだけ puzzles に入れる

ビットマスクにしておけば判定は AND 一発で済む。全体で数秒に収まる。

### 判定の母集団は「全解」

制約が効いているかの判定は、**プレイヤーが入力しうるすべての式**に対して行う。
冗長解も入力できてしまうので、母集団から外すと
「制約をかけたのに、冗長な解で通ってしまう」パズルができてしまう。

curate は行を削除せずフラグを立てるだけなので、`solutions` 全件をそのまま母集団に使える。

- **判定の母集団** → 全解（`is_redundant` を無視）
- **解答例として表示するもの** → `is_repr = 1` の中から選ぶ

### 重複する制約の排除

異なる制約でも残存解の集合が同一になることがある。
**残存解の id 集合をキーにして重複排除**し、`rules` の文字列が短いもの、
次に辞書順で先のものを1つだけ残す。

### 採用条件

```
harder_by >= 1  かつ  制約を満たす解が 1 つ以上あること
```

**5.2 で「制約下で最もラクな解が冗長解でないこと」という条件をやめた。**
5.1 まではこれを採用条件にしていた（制約を満たす解が全部冗長な puzzle を弾いていた）が、
6-5 に置き換えた ―― `is_redundant` は「どれを解答例にするか」「一覧に何本並べるか」を
決める印であって、パズルが成立するかどうかとは別の話である。`0158` の
`0! + 1 ^ 5 + 8` は合法な入力で正解になるので、プレイヤーは解ける。
**解けるパズルを帳簿の都合で消さない。**

`example_solution_id` は冗長でない解から選ぶ。1 つも無ければ制約を満たす全解から選び、
6-5 の印を付ける。`min_score` もそのとき採った解のスコアになる。

**「最もラクな解が冗長解でない」は保証しない。** 「残存解の一部が冗長で、その冗長解が
一番ラク」というケースは素通りする。プレイヤーは冗長な式でも正解にたどり着けるが、
これは意図的に許容する。何も強制されておらず、基本問題（`rule_count = 0`）でも
同じ状況は起きるので、扱いを揃える。

`harder_by = min_score - base_min_score`。制約なしなら簡単な解で済んだのに、
制約によって難しい解しか残らなくなった度合いを表す。

`harder_by = 0` の puzzle は、**制約をかけても制約なしと同じ最も簡単な解が生き残っている**。
プレイヤーからすると制約を読んだ意味がないので、puzzles に入れない。

`survivor_count <= 3` を追加で課すことも検討したが、**採らない**。
候補が12通りしかない今の構成では、この条件を足すとカバー問題数が
8,390 → 6,528 と大きく落ちてしまう。使える制約が少ないぶん、1つずつを大事にする。

### 無制約の puzzle も入れる

`rule_count = 0`、`rules = ""` の行を各問題に1つ入れる。これが基本問題になる。
採用条件は適用しない（全 9,161 問に必ず1件入る）。

### 結果の実測値

（2026-09-13、5.2 の generate〜blob 一巡で確認。過去の値は
2026-09-05 が 基本 9,161 / 制約付き 11,961 / 合計 21,122 / 挑戦モード 2,009、
その前の見積もりが 基本 9,161 / 制約付き 9,091 / 合計 18,252 / 挑戦モード 1,142）

```
基本問題（無制約）:   9,161
制約付き（使用禁止）: 12,565
合計:                21,726
   うち挑戦モード       1,929
```

5.2 で増えたのは 6-2 の廃止と 6-5 の救済によるもの。挑戦モード（難易度17以上）が
2,009 → 1,929 に減ったのは、6-4 の救済で「最も簡単な解」を選ぶようにした結果
`min_score` が下がった問題があるため。

制約付き puzzle は 1問あたり中央値2・最大4 に収まるので、後段の選抜は不要。

制約付き puzzle が1件も作れない問題が 1,798 問あるが、これは正常。
うち 595 問は**そもそも解が1つしかない**ため、どんな制約も
「その1解を残す（＝無制約と同じ）」か「0解にする」のどちらかにしかならない。
残りは、全ての解が同じ演算子を同じ回数使っていて括弧の位置だけが違うケース
（例: 1376 の `1 * ( 3 + 7! / 6! )` と `1 * 3 + 7! / 6!`）で、
演算子の使用回数では区別できない。これらも基本問題としては出題できる。

## 6-D. 演算子の導入（本編の進行で使う）

新しい演算子をプレイヤーに教えるとき、ルールで強制するのではなく
**その演算子を使わないと解けない問題**を出す。行き詰まって自分で気づく形にする。

判定は簡単で、その problem_id の解を全部見て、`cnt_fac = 0` の解が1つも無ければ
「階乗が必須」。`cnt_pow = 0` が無ければ「累乗が必須」。

```
階乗が必須の問題: 2,081問（うち難易度9以下 676問）
累乗が必須の問題:   214問（うち難易度10以下 168問）
```

例:

```
3! + 5 - 8 + 7        画面に制約表示は無いが、3! を使わないと解けない
( 9 - 8 ) ^ 3 + 9     累乗が必須
```

本編では、階乗を26問目あたり、累乗を41問目あたりで3問ずつ連続で出す。

## 7. スコア設計（暫定版・第6-B章の採用条件のために必要）

難易度は人間の感覚に依存するので、これは**暫定値**である。実際にプレイして調整するため、
重みは必ず1箇所の辞書にまとめ、変更したら `annotate` の再実行だけで反映されるようにする。

```python
OP_COST = {"+": 1, "-": 1, "*": 2, "/": 3, "^": 5, "!": 4}

BONUS = {
    "paren": 1,              # 括弧1組につき
    "fraction": 5,           # 途中で分数が現れる
    "zero_factorial": 3,     # 0! を使う（気づきにくい）
    "nested_factorial": 8,   # ( 3! )! のような階乗の2回適用
}
```

解のスコア = 使った演算子のコスト合計 + ボーナス合計。
`solutions.score` に入れる。

問題の難易度（`problems.min_score` / `max_score`）は、その問題の解のスコアの最小・最大。
**最小スコア = その問題を解く一番ラクな方法の難しさ**なので、これが問題の実質的な難易度になる。

この数値は「とりあえず動かすための仮の値」であって、正しさを主張するものではない。
プレイできる形になったら、実際の体感と突き合わせて調整する。

## 8. エクスポート形式

ゲーム本体が読む JSON。`puzzles` の全行を出力する（基本問題 + 制約付き）。

```json
{
  "version": 1,
  "rules": { "pow_assoc": "right", "zero_pow_zero": 1, "double_factorial": false },
  "puzzles": [
    {
      "id": "0004",
      "constraint": null,
      "difficulty": 23,
      "solution": "( 0! + 0! + 0! )! + 4",
      "solution_count": 5
    },
    {
      "id": "6988",
      "constraint": { "op": "/", "count": 1 },
      "difficulty": 25,
      "solution": "6! / ( 9! * 8 ) * 8!",
      "solution_count": 1
    }
  ]
}
```

- `id` … 4桁の数字。同じ id が複数回現れる（制約違いで別のパズルになるため）
- `constraint` … `null` なら基本問題。制約付きなら演算子と回数。
  `count: 0` は「使用禁止」、`count: 1` は「ちょうど1回」
- `difficulty` … その puzzle の残存解の最小スコア（`puzzles.min_score`）
- `solution` … 解答例1つ（`example_solution_id` の `display`）
- `solution_count` … その制約下で正解となる式の総数（`survivor_count`）

`rules` を埋め込んでおくのは、ゲーム側の計算エンジンと生成側の解釈がずれていないことを
起動時に確認できるようにするため。`double_factorial: false` は
「`!!` という記法は受け付けない」ことをゲーム側に伝えるもの（第2章参照）。

### ゲーム側の入力UIに関する制約

エクスポートしたデータは以下を前提にしている。ゲーム実装時に守ること。

- `!!` は入力不可にする。階乗を2回かけるときは `( 3! )!` と括弧で組み立てさせる
- `^` は右結合として計算する
- `0 ^ 0` は 1 とする

## 8-B. BLOB（ゲーム本体に埋め込む形式）

`export` の JSON とは別に、`docs/index.html` の中へ直接埋め込む軽い形式も持つ。
ゲーム本体は単一 HTML なので、**外部ファイルを読まずに済ませる**ためこちらを使う
（`export` の JSON は人間が中身を見るためのもの）。`blob` サブコマンドが
`index.html` のテンプレートリテラル `const BLOB=` の中身を差し替えるので、
手作業のコピーは要らない。

```
§COURSE
8842,N,3,12,0,0,8 + 8 - 4 - 2
...
§FREE
0004,M0,23,1,1,0,( 0! + 0! + 0! )! + 4
...
§CHAL
0004,N,21,2,1,0,( 0! + 0! ) * ( 0! + 4 )
```

1 行 1 puzzle で `id,rc,d,n,nf,np,sol`。

- `id` … 4桁の数字
- `rc` … 制約。`N` は制約なし。`A0` `S0` `M0` `D0` `P0` `F0` は
  それぞれ `+` `-` `*` `/` `^` `!` の使用禁止（記号の対応は index.html の `OPOF`）
- `d` … 難易度（`puzzles.min_score`）
- `n` … その制約下で正解となる式の総数（**`puzzles.survivor_count`**）
- `nf` / `np` … 階乗／累乗を使わないと解けない問題か（0 か 1。第6-D章）
- `sol` … 解答例（`example_solution_id` の `display`）

`sol` に `,` は出てこないが、読む側（index.html の `parseBlob`）は 7 個目以降を
`,` で繋ぎ直すので、仮に入っても壊れない。

### 3 区分

`puzzles` の全行を**重複なく** 3 つに分ける。和集合が全 puzzle になる。

```
CHAL    1,929 問   min_score >= 17 の全部（挑戦モード）
COURSE  1,000 問   残りから選抜規則で選ぶ（本編。GAME-SPEC 7-1）
FREE   18,797 問   残りの全部（フリープレイ・べつの問題）
                   合計 21,726 問
```

（実測値。2026-09-13、5.2 の `blob` 一巡で確認）

ゲーム側の「N / 1929」「N / 1000」はすべて `CHAL.length` / `COURSE.length` から
描くので、件数を直値で持っている箇所は無い。

### COURSE の選抜

`select_course()`。**決定的**で、乱数を使わず `COURSE_SEED` と位置と問題から
ハッシュで順位を付ける。同じ DB からは何度流しても同じ 1,000 問になる。
位置ごとに「段の条件（`_course_stage_cond`）」「目標難易度（`_course_target`）」
「制約つきを置くか（`_course_want_constrained`）」を満たす候補を集め、
順位が最小のものを採る。候補が無ければ難易度の許容幅を 1 ずつ広げ、
最後の手段として「直近 10 問に同じ 4 桁を出さない」を外す
（現状は 1,000 問すべて拡張 0 回で埋まる）。

### blob の検証（12 項目）

**生成と同じ実行の中で**回し、1 つでも落ちたら `index.html` を書き換えない。

1. 件数と分割 … COURSE 1,000、3 区分の `(id, rc)` が `puzzles` 全行と一致（取りこぼし・重複 0）
2. 難易度の境目 … CHAL は `d >= 17`、COURSE と FREE は `d <= 16`
3. COURSE の段の条件 … 7 段すべて充足、1〜50 が全部制約なし、許容幅の拡張が 0 回
4. COURSE の重複と間隔 … `(id, rc)` の重複 0、同じ 4 桁が 10 問以内に再登場しない
5. DB との一致 … 全行の `d` / `n` / `nf` / `np` / `sol` が DB と一致
6. 解答例の再評価 … 全行の `sol` を**独立実装の評価器**（`_plain_eval`。`build_evaluator`
   とは別実装）で読み直して 10 になり、使われている数字が `id` と順序どおり一致する
7. ★ の導入位置 … 26〜28 が階乗必須かつ引数 >= 3、41〜43 が累乗必須かつ `^` が効いている
8. 埋め込みの安全性 … 全行が 7 フィールド、`` ` `` / `${` / `CR` / `\` を含まない
   （テンプレートリテラルの中に入れるため）
9. 難易度カーブ … COURSE を 100 問ずつのブロックに切り、平均難易度が**単調非減少**
10. 後半の難易度 … 201 問目以降（800 問）が全部 `d` 9〜16
    （`_course_floor` が 200 問目で 9 に達し、`_course_target` が 16 で頭打ちになる帰結）
11. 難易度の範囲 … 全区分の全行が `d` 3〜44（GAME-SPEC 6-2）。
    区分の境目を見る 2 とは別に、上下の端を見る
12. 再現性 … **同じ DB・同じ `COURSE_SEED` でもう一度 BLOB を組み、バイト単位で一致**するか。
    `_blob_build()` を 2 回呼んで突き合わせる（DB から読み直す独立した組み立て）

9〜12 は 5.3 で追加（それまでは 8 項目だった）。

## 9. 実装方針

- **ユーザーとのやりとりはすべて日本語で行う。** 作業報告、確認の質問、選択肢の提示、
  進捗メッセージ、表の見出しに至るまで日本語で書くこと。コード内のコメントも日本語。
  変数名・関数名・コミットメッセージは英語でよい
- Python 3。**標準ライブラリのみ**（`fractions`, `sqlite3`, `json`, `argparse`, `csv`）。外部依存を入れない
- 浮動小数点は一切使わない
- 定数（上限値、スコアの重み）はファイル冒頭にまとめる。コード中に数字を散らさない
- 各サブコマンドに対して、小さなテストを用意する。特に `verify` は既知の落とし穴を
  テストケースにする:
  - `0 ^ 0 ^ 0` が `(0^0)^0` ではなく `0^(0^0)` と解釈されること
  - `2 ^ 3!` が `2^(3!)` = 64 であること
  - `( 2 ^ 3 )!` が 40320 であること
  - `3!!` が **構文エラーになること**（二重階乗と混同されるため出力も入力も禁止）
  - 階乗を2回適用した木が `( 3! )!` とレンダリングされ、`3!!` にはならないこと

## 10. 旧データについて

旧世代の CSV（`make10_analysis*.csv`, `make10_levels_master_*.csv`, `make10_problems*.csv`,
`make10_score_ranges.csv`）は 3 世代が混在しており、信頼できない。
新パイプラインの出力と照合する目的以外では参照しないこと。
`*_with_id.csv` は全行 `UNKNOWN` で完全に無効なので削除してよい。
