# Make10 ゲーム仕様書

レビュー用。現行の試作（`index.html`）が実装している内容を記述する。
「仕様がこうなっているか」を確認できる粒度で書いた。

対象: Android アプリ（Capacitor で WebView 化して Google Play で配布予定）。
現状はブラウザで動く単一 HTML の試作。

---

## 1. ゲームのルール

4つの数字（各 0〜9）が画面に**固定の順序**で並ぶ。プレイヤーは演算子を配置して、
計算結果を **10** にする。

- 数字の並び替えは不可
- 数字の連結（`1` と `2` で `12` を作る等）は不可
- 使える記号: `+` `-` `*` `/` `^`（累乗） `!`（階乗） `(` `)`
- 二項演算子は**数字と数字の間にちょうど1つずつ、合計3つ**配置する

問題によっては「この演算子は使えません」という制約が付く。

---

## 2. 計算仕様（最重要）

### 2-1. 数値表現

**すべて有理数（分数）で厳密に計算する。浮動小数点は使用禁止。**
分子・分母は多倍長整数（JavaScript の `BigInt`）。

`10` の判定は「分母が 1 かつ分子が 10」で行う。誤差許容による判定はしない。

### 2-2. 文法

```
expr    := term  (('+' | '-') term)*
term    := power (('*' | '/') power)*
power   := postfix ('^' power)?          ← 右結合
postfix := atom '!'?                     ← '!' の連続は構文エラー
atom    := DIGIT | '(' expr ')'
```

優先順位は低い順に `+ -` < `* /` < `^` < `!`（後置）。

### 2-3. 累乗は右結合

```
a ^ b ^ c  は  a ^ (b ^ c)      ← 数学の標準
```

**過去にここでバグを出している。** 内部の木構造は正しいのに、文字列に変換するときに
括弧を落として `(0^0)^0` を `0 ^ 0 ^ 0` と表示していた。194,445件中470件が誤りだった。

したがって次が必須要件になる:

> **表示用の文字列は、標準的な記法で読み直したときに元の木と同じ意味になること。**

左オペランドが `^` のときは必ず括弧を付ける（`( 0 ^ 0 ) ^ 0`）。
右オペランドは右結合なので括弧不要。

### 2-4. 階乗の表記

このゲームで使うのは**単項の階乗を繰り返し適用する**操作であり、二重階乗ではない。

```
( 3! )!  = 6! = 720      ← 使うのはこちら
3!!      = 3 x 1 = 3     ← 数学の標準記法における二重階乗。使わない
```

- **`!!` という並びを表示してはいけない。** 必ず `( 3! )!` と括弧で書く
- **パーサは連続する `!` を構文エラーとして拒否する**（再発検出のため）
- 入力UIでも `!` を既存の `!` の直後・直前には置けないようにする

### 2-5. 定義と上限

| 項目 | 仕様 |
|---|---|
| `0 ^ 0` | `1` |
| `0 ^ 負数` | 無効 |
| 指数 | 整数のみ。絶対値 24 まで。非整数の指数は無効 |
| 階乗の引数 | 非負整数のみ。**12 まで**。負数・分数への階乗は無効 |
| ゼロ除算 | 無効 |
| 中間結果への階乗 | 許可（`( 1 + 4 - 5 )!` など） |
| 括弧の在庫 | `(` `)` それぞれ 4 個まで |

「無効」＝ 式全体が計算不能。結果欄に「この式は計算できません」と表示する。

上限値の根拠（旧データ 534,163 件の実測）:

- 階乗の引数が 11 は有効な解が存在する（`5! - ( 2 + 9 )! / 9!`）。12 以上は 0 件
- 指数 24 は有効（`( 0 - 0! ) ^ 4! + 9` = `(-1)^24 + 9`）。それ以上は不要

---

## 3. 正誤判定

次の**すべて**を満たしたらクリア。

1. 二項演算子が 3 つ配置されている
2. 括弧の対応が取れている
3. 式が計算可能（2-5 の「無効」に当たらない）
4. 計算結果が厳密に 10
5. 制約を満たしている（制約付き問題の場合）

結果の表示は **1 と 2 を満たしたとき**に行う。満たさない間は何も表示しない
（「演算子を式の中へ」等の案内のみ）。

制約は**「指定された演算子を 1 回も使わない」形式のみ**（6-1）。「ちょうど N 回使う」
のような回数指定は無い。

制約違反の式は**そもそも組めない**。禁止された記号はトレイでグレーアウトされ、
`off` 状態のチップは取り出そうとすると揺れるだけで配置に進まない（5-1）。式に入る
経路が塞がれているので、通常のプレイで「◯ は使えません」という不正解表示が出ることは
ない。判定側（結果が 10 でも使用回数が 0 でなければ不正解）にも制約チェックは
あるが、これは**入力側の保証が崩れたときの保険**であり、コードは残す。

（A-4〈2.6〉で消した「回数バッジ」の分岐は、実装されていない機能があるように見える
死んだコードだったので削除した。こちらは正誤判定そのものなので、到達不能でも残す。）

### 3-1. 記録は正解した瞬間に 1 回だけ確定する

上の条件を満たした瞬間にスコアを計算し、**その場で記録（最高得点・履歴・統計）を
確定する。そのあと盤面をどう触っても、記録もスコアも変わらない。**

- 正誤の判定は描画のたびに行われるが、**記録するのは最初の 1 回だけ**。
  式を崩して組み直しても再確定しない
- 「つぎへ」は**進行だけ**を行う（本編の位置を進める・保留リストを動かす）。
  2 回目以降の「つぎへ」は無視する
- 正解したあとホームに戻っても**記録は残る**。ただし本編の位置は進まない。
  次の問題へ移るのは「つぎへ」を押したときだけ
- 「全部消す」を押しても記録は消えない（4-3）

3.0 までは記録が「つぎへ」の側にあった。正解カードが出し直される経路と重なると
進行処理が 2 回走り、**解いていない問題が記録されて本編が 1 問飛ぶ**不具合があった。

---

## 4. 操作仕様

### 4-1. 基本

- **ドラッグが既定**。設定で**タップ配置**に切り替えられる
- ドラッグ中、配置できる位置に**隙間が開き、薄い色の枠**が表示される
- 枠から 40px 以内で指を離せば、最も近い枠に吸着する
- **吸着すると隙間が広がり、色も明るくなる。** 広がりは**吸着した 1 つの枠の width**
  で表現する。flex の中央寄せ（`.exprwrap` の `justify-content:center`）が効くので、
  左右対称に開いて見える
- 枠が**開く／閉じる**ときの時間は `--dur`、**吸着する／外れる**ときの時間は
  `--hotDur`（`.zone.snap` クラスで width の transition-duration を切り替える）
- 枠を開くときに複数の枠が同時に width アニメーションすることは無い。隙間の幅は
  CSS 変数 `--zoneW` で一括指定し、縮小率は事前に算術で求める（4-5 参照）

タップ配置モードでは、トレイの記号を押して選択 → 置き場所を押す。
式の中の記号を押すと外れる。**ただし記号を持っている間は外れない。** 持っている状態は
「置く」意図が確定している場面であり、しかもこの消去は手数に残らず後から追えないため。
外したいときは、**トレイの同じ記号をもう一度押して選択を解いてから**押す。

### 4-2. 配置できる位置

トークン列に対する挿入位置ごとに判定する。`前` `後` は挿入位置の前後のトークン。

| 記号 | 条件 |
|---|---|
| 二項演算子 | 前が「数字 / `!` / `)`」かつ 後が「数字 / `(`」。かつ、その数字間にまだ演算子が無い |
| `!` | 前が「数字 / `)`」。かつ 後が `!` でない（`!!` 禁止） |
| `(` | 後が「数字 / `(`」。在庫が残っている |
| `)` | 前が「数字 / `!` / `)`」。在庫が残っている |

`(` `)` は**片方ずつ**配置する。演算子を1つも置いていない状態でも、数字と数字の間に
括弧を差し込める（括弧を先に組んでから演算子を入れる進め方を許す）。

**挿入後のトークン列が同一になる候補は 1 つにまとめる（最も右を残す）。** 既存の
括弧の左右は同じ列になる（`1 ) 9` の `)` に `)` を足すと、左右どちらから入れても
`1 ) ) 9`）ので、枠は片側にしか出ない。括弧が連続していれば、その並び全体で
枠は 1 つ。他の記号は配置ルールにより同じ記号が隣接しないので、この統合は起きない。
実装は「括弧の隣なら消す」という特別扱いではなく、候補ごとに挿入後の列を作って
比較する（配置ルールを変えても自動的に正しくなるように）。

### 4-3. 削除・上書き

- 配置済みの記号を**式エリアの外へドラッグ**すると外れる。枠が赤くなり案内が出る
- 「全部消す」ボタンで**式だけ**を初期状態に戻す。
  **経過時間・手数・ヒントを見た記録は引き継ぐ**（次の問題を読むのとは別の処理）。
  手際の評価は `手数 − 盤面のトークン数`＝「置いては外した空回り」を見るもので、
  全部消しは空回りそのもの。ここで 0 に戻すと、行き詰まったら全部消せば
  手際の減点を避けられてしまう
- **二項演算子は、既に置かれている演算子の上に落とすと差し替わる。**
  差し替えできる演算子は、**記号を明るくして下線を 1 本**引いて示す
  （トレイで選択中のチップと同じ表現。面は塗らない）。この強調は
  **ドラッグ中だけでなく、タップ配置で記号を選んでいる間も出る**
- 差し替えにも**距離の許容がある。** 指が直接触れている要素があればそれを優先し、
  どこにも触れていないときは**隙間の枠と差し替えを同じ距離で比べて近いほうを採る**
  （同距離なら枠）。許容距離は枠と差し替えで別々に持つ（既定はどちらも 40px）。
  距離を見ずに要素への直撃だけで判定していた頃は、演算子の箱を 1px 外れた瞬間に
  隣の枠へ持っていかれ、演算子が 3 つ埋まっているときは数字の上が完全な死に地だった
- **式が変わらない差し替え（同じ記号を重ねただけ）は手数に数えない。**
  手際の評価は「置いては外した空回り」を測るもので、何も変えていない操作を
  空回りとして減点するのは筋が違う

### 4-4. 括弧の状態表示

| 状態 | 表示 |
|---|---|
| 対応が取れている | 通常色 |
| 相手がいない | **コーラル色**。結果欄に「括弧が対応していません」 |
| 対応は取れているが計算に効いていない | **グレー**（式が完成しているときのみ判定） |

「効いていない」の判定は**構文木の比較**で行う。その括弧の組を取り除いて構文解析し、
木が変わらなければ冗長。値の比較ではないので、偶然値が一致するだけの括弧を
誤判定しない。冗長でも正解としては受理する。

### 4-5. 式の横幅

**式は折り返さない。** 幅に収まらない場合、式全体を `transform: scale()` で縮小する
（下限 50%）。枠の開閉アニメーション中も追従する。

「追従」は、**毎フレーム `offsetWidth` を測り直す方式ではない。** 枠が開いた（閉じた）
後の最終的な幅を先に算術で求め、縮小率を枠の開閉と同じ時間・同じ曲線で同時に
遷移させる（式が一瞬はみ出してから縮む、という見え方をなくすため）。全トークンの
幅は固定値（JS の `TOKEN_METRICS`）なので、DOM を測らずに求まる。数字の箱幅は
実機で詰めて `normal` 28px / `compact` 23px、左右マージンは 2px / 1px に確定した。
CSS 変数への流し込みと `tokensWidth()` の算術は同じ `TOKEN_METRICS` を見るため
食い違わない（開発者パネルの「幅の整合」で見張る）。

数字の箱幅を詰めても、記号を多く置いた式ではトークン幅が使える幅を超える。
このとき式全体が縮小される（実測で 85% 程度。下限 50% に対して余裕がある）。
これは想定された動作で、読みにくさは無いと判断した。`#app` の `max-width`（430px）を
広げれば大型端末では緩和されるが、狭い端末には効かず、タブレットで間延びするため
採用しない。**縮小がかかること自体は不具合ではない。**

**縮小より先に、隙間の幅を詰める。** ドラッグ中に開く隙間の幅（`--zoneW`）は固定
ではなく、開く枠の数と残り幅から 1 回だけ計算し、CSS 変数に書き込む。

```
Δ       = 吸着で広がる量（下記）
隙間幅  = clamp(ZONE_MIN, (使える幅 - トークン幅 - Δ) / 枠の数, ZONE_BASE × 通常幅比)
```

- こうすると、`(` のように開く枠が多い記号でも**数字は縮まないまま**収まる
- `ZONE_MIN`（既定 20px）でも収まらないときに限り、上の縮小に頼る。狙う場所が
  小さくなるのを避けるための措置で、演出ではなく操作性の要件

**吸着で広がる量 Δ の予約。** 吸着した枠は width を `--zoneW + Δ`（CSS 変数
`--zoneHotW`）まで広げる。同時に吸着できる枠は 1 つだけなので、縮小率の計算には
最初から `+ Δ` を含めておく。こうすると**吸着してもしなくても縮小率は変わらず**、
ドラッグ中に数字の大きさが動くことが無くなる。

**開発者パネル。** `--dur` / `--hotDur` / Δ / `ZONE_MIN` / 通常幅比 / 吸着の判定半径 の
6 本は、設定画面の隠しパネル（開発用の解放後に出る）のスライダーで即時に調整でき、
値は `localStorage` に保存される。実機で詰めた値をコードの初期値（`DEV_DEFAULT`）へ
反映する運用。数字の箱幅・左右マージンも一時スライダーで詰めたが、値が確定したので
撤去し `TOKEN_METRICS` の直値にした。パネルには「縮小せずに済む隙間幅の上限」「数字
0〜9 の実描画幅（両モード）と片側余白」「`tokensWidth()` と `.expr` 実測幅の一致」も
表示される（後 2 つは `TOKEN_METRICS` を触ったときの検算用に残す）。

パネルを開くと問題画面が `display:none` になり `offsetWidth` / `clientWidth` が 0 を返す
ため、**パネルは開いた時点では何も測らない。** 問題画面が見えているあいだに測った値を
退避しておき、パネルはそれを**計測時刻付き**で表示するだけにする。隙間まわりは
`layoutZones()`（ドラッグ計算時）が、幅の整合は `hideZones()`（枠を閉じる直前。live 枠の
実測幅を引いてトークンだけの幅にする）が退避する。まだ一度も測っていなければ「未計測」と
出す（0 や負の値は出さない）。

---

## 5. 画面構成

プレイ画面。上から順に。

```
┌────────────────────────────────┐
│ [ホーム(丸)]        [三本線メニュー(丸)]   │  ← ヘッダー。左右にアイコンのみ
│ 計算結果                              │
│ 式エリア（ドロップ先。上下に伸縮）          │
│ ヒント（ヒントボタンで開く。浮かせ表示）      │
│ [全部消す(丸)]            [ヒント(丸)]     │  ← 中央は空き
│ 演算子トレイ（角丸パネル。4列×2行）        │
│ 広告バナー（52px 固定）                 │
└────────────────────────────────┘
```

- 画面高さは `100dvh`。**スクロールは発生させない**。式エリアが伸縮して高さを吸収する
- 高さ 700px 以下の端末では、数字・記号のサイズと余白を自動的に詰める
- ヒント（と、本来は階乗表も）は**浮かせ表示**。開いてもトレイの位置は動かない
- 問題コード・難易度・正解数の表示、制約バナー（「×は使えません」の帯）、保留ボタン、
  階乗表を開くボタン、スキップボタンは **DOM とハンドラは残っているが、現行の
  モックアップでは非表示**。制約の提示はトレイのグレーアウトのみ（5-1）
- 非表示は CSS の `#rule,.top .info,.trayrow,#hold,#alt{display:none!important}` で行う。
  `!important` は `.hide` など他クラスとの優先順位を確実に上回るため。
  JS 側の定数 `HIDDEN_UI={rule,info,factTable,hold,alt}` がこれと対で、
  「機能の状態を復元しない」判断（例: 5-2 の階乗表）に使う。表示を戻すときは両方を直す

### 5-1. 制約の表示

制約は**「使用禁止」の 1 種のみ**（3・6-1）。「ちょうど N 回」等の回数指定は無いので、
回数バッジのような表示も持たない。

| 制約 | トレイでの見た目 |
|---|---|
| 使用禁止 | 記号をグレーアウト（背景も斜線も出さない）。押すと**揺れる**（配置不可を示す） |

制約は1問につき最大1個。

### 5-2. 階乗表

`0!` 〜 `12!` の値を一覧表示する。開閉状態は保存される。

ただし現行モックアップでは開閉ボタン（`#factbtn`）を含むトレイ行を非表示にしている
（5 章の非表示要素。JS 側の `HIDDEN_UI.factTable` と対）。**非表示にしている間は
起動時に開閉状態を復元しない**（保存値そのものは書き換えず保持する）。開いた状態で
保存された端末で、閉じる手段が無いまま開きっぱなしになるのを防ぐため。トレイ行の
表示を戻すときは CSS の `display:none!important` と `HIDDEN_UI` を同時に変える。

---

## 6. 問題データ

### 6-1. 種類

```
基本問題（制約なし）     9,161 問
制約付き（使用禁止）    11,961 問
合計                  21,122 問
   うち難易度17以上      2,009 問（挑戦モード）
```

（実測値。2026-09-05、_render の括弧欠落修正後の generate〜export 一巡で確認）

問題は 0000〜9999 の 1 万通りのうち、解が存在する 9,161 通りが元になっている。
同じ数字でも制約が違えば別の問題として扱う。

制約は**「使用禁止」のみ**。「ちょうど N 回使う」形式は採用しない
（手順を指示される感覚がパズル体験を損なうため）。

### 6-2. 難易度スコア（暫定）

```
演算子コスト  + 1 / - 1 / * 2 / / 3 / ^ 5 / ! 4
ボーナス     括弧 1組につき +1
             途中で分数が出る          +5（0/1判定）
             0! を使う                +3（0/1判定）
             ( 3! )! のような階乗の2回適用 +8（0/1判定）
```

問題の難易度 = **その問題を解く最もラクな方法のスコア**。範囲は 3〜45（実測値）。

重要な点として、難易度と解答例は**「冗長でない解」からのみ**算出する。
`x * 1` や `x + 0` のような値を変えない演算を含む解は、正解としては受理するが
難易度の基準にはしない。両者の出どころが食い違うと、
「難易度8と表示されているのに解答例が15点の式」という矛盾が起きる。

このスコアは**暫定値**。プレイして調整する前提なので、重みは1箇所にまとめること。

### 6-3. 問題コード

各問題に 5 文字のコードが割り当てられる。友達に送ると同じ問題が開く。

```
6988（÷禁止）  →  例: Z2JN7
```

- 数字4桁と制約を 18bit に詰め、Feistel 構造で撹拌し、7bit のチェックサムを付けて
  25bit を Base32（5文字）にする
- **数字も制約も見た目から読み取れない。連番も無関係な文字列になる**
- でたらめな 5 文字が有効と判定される確率は約 0.4%
- 紛らわしい `0 O 1 I L` は文字セットから除外。入力時に自動で読み替える

**ソースを読めば復元できる**（クライアント完結のため）。ランキング等を実装するなら
サーバー側の検証が別途必要になる。

---

## 7. モードと進行

| モード | 内容 |
|---|---|
| 本編 | 固定 1,000 問。難易度が段階的に上がる。進捗を % で表示 |
| フリープレイ | 難易度16以下からランダム。8割は難易度9以上、2割は全体から |
| 挑戦モード | 難易度17以上。**累計100問クリアで解放** |
| 保留 | 保留した問題を後から解く。**最大30問** |

### 7-1. 本編のカーブ

```
  1〜 4問目   足し引きのみ（括弧・累乗・階乗なし）
  5〜10問目   ×÷が入る（同上）
 11〜20問目   括弧も出てくる
 21〜25問目   四則の組み合わせ
 26〜28問目   ★階乗を使わないと解けない問題
 29〜40問目   階乗ありも混ざる
 41〜43問目   ★累乗を使わないと解けない問題
 44問目以降   段階的に難易度を上げ、最終的に難易度9〜16の帯へ
```

★の導入問題は**制約で強制しない**。「その演算子を使わないと解けない問題」を選ぶことで、
プレイヤーが行き詰まって自分で気づく形にする。

### 7-2. 挑戦モードのロック

ホーム画面に**最初からロック状態で表示**し、右端に **`あと N 問`** と残り数を出す
（3.0 で「あと N 問で解放（難易度17以上）」から短縮。難易度の条件は「遊び方」に記載）。
ロック中に押しても**反応しない**（以前は揺れる演出があったが、`.mode` 要素に
対応する CSS が無く効いていなかったため廃止）。存在を隠さないことで目標として機能させる。

### 7-3. 保留

- 上限 30 問。超えた状態で保留しようとすると警告
- 保留リストは**問題コードで保存**する（データが増えても壊れないため）

---

## 8. 正解演出

難易度を主軸に 3 段階。

| 段階 | 条件 | 演出 |
|---|---|---|
| 1 | 難易度 8 以下 | 数字が順に跳ねる。リングが1つ広がる |
| 2 | 難易度 9〜13 | ＋演算子が順に光る。リングが2重 |
| 3 | 難易度 14 以上 | ＋背景の格子が走る。「お見事」表示 |

60秒以上かかった場合のみ 1 段階上がる（上限 3）。

**時間を主軸にしない**理由: 時間で演出が変わると気づいたプレイヤーが、
答えが分かっていても放置して待つようになる。報酬が「難しさ」ではなく
「待ち時間」に付いてしまう。

対応端末では振動も出す。

### 8-1. 正解カードの扱い

- 正解カードが出ている間も**盤面は触れる**。式を崩しても組み直しても、記録と
  スコアは正解した瞬間のまま変わらない（3-1）
- 設定・統計などへ移ると正解カードは隠れるが、**問題画面に戻ると出し直す**。
  中身は正解時に確定した内容をそのまま出す（計算し直さない）。
  点数を出すかどうかだけは、出すたびに設定を見る
- 「全部消す」を押しても**カードは出たまま**。記録と同じく盤面の内容とは独立している
  （隠すと「つぎへ」が無くなり、先へ進めなくなる）

---

## 9. 保存データ

`localStorage` に 1 キーで JSON 保存（アプリ版では Capacitor Preferences 等に置換予定）。

```
本編の進行位置
累計クリア数
保留リスト（問題コードの配列）
問題ごとの最高得点・クリア履歴・統計
問題ごとに見たヒントの段階
タップ配置モードのオン／オフ
階乗表の開閉状態
```

**ヒントを見た記録は問題ごとに残り、解き直しても消えない。** セッション内の変数だけで
持つと、「全部消す」・ホームに戻って入り直す・アプリを再起動する、のどれでも 0 に戻り、
ヒントの倍率が元どおりになる。最高得点は高いほうだけを残すので、抜け道を一度でも
通ればその点数が永久に残ってしまう。**一度ヒントを見た問題は、その後どう解いても
満点にはならない。**

ホーム画面に「進捗をリセット」ボタンがある（2回押しで実行）。ヒントを見た記録も
他の進捗と同じく消える。

---

## 10. 収益化（設計中）

| 手段 | 内容 |
|---|---|
| バナー広告 | 画面下部に固定（52px）。トレイとの間に余白を確保する |
| リワード広告 | 段階ヒント、保留枠の一時拡張 |
| 買い切り課金 | 広告除去 |

**インタースティシャルは入れない。** 問題を解くリズムを断ち切る影響が大きいと判断した。

ヒントは 3 段階（使う演算子 → 式の形 → 正解の式）で実装済み。**見た段階は端末に残り、
解き直しても消えない**（9）。リワード広告でヒントを配るなら、見た事実が残っていないと
成立しないため。

---

## 11. 現行試作の制限（本番との差分）

レビュー時に留意してほしい点。

- 問題データは **3,300 問だけ埋め込んだ抜粋**。本番は 21,122 問
- 広告バナーは**ダミーの帯**。実装なし
- ヒントは段階化されていない
- 課金は未実装
- 開発者用の解放がある（タイトル7回タップ、または合言葉入力）。リリース時は要検討
- 単一 HTML ファイルの手書き試作。本番は部品分割が必要
- **書体は Zen Kaku Gothic New（ウェイト 400 / 500 / 700）。** Google Fonts から
  読み込んでおり、Capacitor 化の際にアプリへの同梱が必要。日本語全体を含むため
  サブセット化の検討が要る。
  （太字は 800 を使わず 700 に統一。式中の演算子 `.tok` は `font-weight:600` 指定
  だが Zen Kaku に 600 が無いため 700 に解決され、数字と同じ太さになる）

---

## 12. 過去に踏んだ罠（同じ失敗を避けるため）

実装をレビューするなら、ここを重点的に見てほしい。

1. **累乗の結合順** — 計算は正しいのに表示で括弧を落としていた（470件が誤り）
2. **`3!!` と `( 3! )!`** — 記法の混同。しかも検証用パーサが同じ前提を共有していたため検出できなかった
3. **恒等演算の扱い** — `1 ÷ 1` のように「演算子を置いただけ」の解が正解になっていた
4. **難易度と解答例の食い違い** — 算出元が別だった
5. **`*` の右の子が `/` のとき括弧が落ちていた** — `4 * ( 9 / 4 )` を `4 * 9 / 4` と
   表示していた（左結合の演算子は同順位の右の子にも括弧が要るのに、`-` `/` しか
   対象にしていなかった）。`a * (b/c)` と `(a*b)/c` は値が一致するので、値だけを
   見る検証では検出できず、往復チェック（display を独立パーサで読み直した木と
   shape の木を比較する）が無かったため 3,433 件が本来より score +5 点になっていた

特に 2 と 5 の教訓として、**検証は生成とは独立に実装する**こと。同じ前提を共有した
検証は検証にならない。値が一致するバグは値ベースの検証では原理的に検出できない
ので、木構造そのものを比較する往復チェックが要る。
