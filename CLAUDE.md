# Make10 開発の指示

4つの数字（0〜9、順序固定）の間に四則演算・累乗・階乗・括弧を入れて
10 を作るパズルゲーム。Capacitor で Android アプリ化し
Google Play で配布予定。

## ファイル構成

| パス | 内容 |
|---|---|
| docs/index.html | ゲーム本体。単一 HTML の試作。GitHub Pages の配信元 |
| docs/v27.html / docs/v28.html | 比較用に残した過去版（2.7 / 2.8）。3.0 の実機確認が済むまで残す |
| GAME-SPEC.md | ゲームの仕様書 |
| DESIGN.md | デザイントークン（配色・文字・寸法・動き・リスト画面のルール） |
| DATA-SPEC.md | データ生成の仕様書 |
| make10.py | 問題データ生成 |
| test_make10.py | make10.py の単体テスト |
| verify_render_roundtrip.py | 生成済み DB の検証（GAME-SPEC 12 章の往復チェック）。`solutions.display` を独立実装のパーサで読み直し、その木から値・`uses_fraction`・`score` を計算し直して DB の値と突き合わせる。make10.py は一切参照しない |
| verify_ui.py | **ゲーム本体の回帰検証（常設・5.4）。** headless Chrome で docs/index.html を開き、normal / compact の両方で回す。使い方は「画面の検証」節 |
| uiharness/ | verify_ui.py の中身。`cdp.py`（標準ライブラリだけの CDP クライアント）・`ui.py`（ケースから使う道具）・`cases/*.py`（1 ファイル 1 主題の回帰ケース） |
| make10.db | 生成済みの問題データ（生成物。**`.gitignore` で除外**） |
| make10_puzzles.json | make10.db から書き出す、ゲーム本体が読む JSON（生成物。形式は DATA-SPEC 8 章） |
| make10_backup_pre52e.db | **generate をやり直さずに make10.db を復元するための DB**（`solutions` / `problems` は現行と同じ・`puzzles` は 5.2 前の 21,709 件）。複製に `annotate → curate → constrain` を流すと約 21 秒（実測 20.8 / 21.8 秒）で、3 表とも make10.db とハッシュ一致を確認済み（**`.gitignore` で除外**） |
| make10_backup_pre55.db | 5.5 の作業前（2026-09-20）の `make10.db` の複製。5.4 の `puzzles`（21,726 件）を持つ。5.5 の組み直しと比べるときに使う（**`.gitignore` で除外**） |
| make10_backup_pre56.db | 5.6 の作業前（2026-09-23）の `make10.db` の複製。5.5 の `score` と `puzzles`（17,791 件）を持つ。10a/a の加点の前後を比べるときに使う（**`.gitignore` で除外**） |
| make10_backup_pre57.db | 5.7 の作業前（2026-09-25）の `make10.db` の複製。5.6 の `score` と `puzzles`（17,793 件）を持つ。階乗の比の 3 規則の前後を比べるときに使う（**`.gitignore` で除外**） |
| make10_backup_0099.db | 8/31 の古い版の generate が 0000〜0099 だけを出した途中の DB（`solutions` 4,138 行・`problems` 0 行）。役目は終わっているが削除の指示待ち（**`.gitignore` で除外**） |
| assets/icons-ref/ | アイコンの参考画像 |
| assets/mockup-play-screen.png | 問題画面のモックアップ |
| __pycache__/ | Python のキャッシュ（**`.gitignore` で除外**） |

## ファイルの配置

- ゲーム本体は `docs/` 配下にある（`docs/index.html`）。仕様書・データ生成・DB は
  リポジトリ直下。**`docs/` の中と外に同名のディレクトリを作らない**
  （過去に `assets/fonts/` を直下だけ消して `web/assets/fonts/` を残す取り違えをした。
  当時はゲーム本体が `web/` 配下にあり、その後 `docs/` に改名した。
  ディレクトリ名は変わったが、この失敗の教訓はそのまま当てはまる）
- ファイルを削除・移動したときは、**同名のものが他の階層に残っていないか `find` で
  確認してから**完了報告を書く
- GitHub Pages は `main` ブランチの **`docs/` フォルダ**から配信される。
  手元の `docs/index.html` がそのまま公開されるので、
  **コピーやアップロードの手作業は要らない**
- 公開 URL は https://ihudodo-k.github.io/make10/ 。
  `docs/v27.html` `docs/v28.html` も同時に公開され、
  `.../v27.html` `.../v28.html` で比較できる

## 毎回守ること

### バージョン管理

- このプロジェクトは Git 管理下にある。**作業を始める前に `git status` で
  作業ツリーがきれいなことを確認する。** 未コミットの変更が残っていたら、
  何が残っているかを報告してから作業に入る
- **完了報告に `git status` も貼る。** `git diff --stat` は**新規の未追跡ファイルを
  出さない**ので、それだけでは「増えたファイル」を見落とす。削除されたファイルも同じ
- **完了報告に `git diff --stat` と `git diff` の出力を貼る。**
  報告文の説明より、実際に変わった行のほうが正確なため
- **指示されていないファイルを作らない・消さない・改名しない。**
  必要だと判断したら、実行せずに提案として報告する。指示書やデータの複製が黙って
  増えると、どちらが正なのか後から判断できなくなるため
- **コミットはしない。** 変更を残したまま報告する。コミットするかどうかは
  こちらが差分を見てから判断する
- `git push` も行わない

### バージョンと変更履歴

- index.html に変更を加えたら **必ず APP_VERSION を上げる**
- 小数第一位を 1 ずつ上げる（1.4 → 1.5、1.9 の次は 2.0）
- 1 回の作業で上げるのは 1 回だけ
- このファイルの変更履歴に 1 行追記する
- **過去の変更履歴の行は書き換えない。当時の記録として残す**

### 仕様書を実装と一緒に直す

- 挙動・画面・データ件数を変えたら、**同じ作業の中で**
  GAME-SPEC.md または DATA-SPEC.md を直す
- **完了報告に「どの仕様書のどこを直したか」を必ず書く**
- 仕様書を直さずに実装だけ終わらせない

### 原因を特定してから直す

- 不具合を直すときは、**原因を特定して報告してから**修正する
- 推測で書き換えない
- 「たぶんここだろう」で複数箇所を同時に変えない

### 性能は計測で判断する

- 「transform だから速い」のような机上判断で決めない
- 変更後に Chrome DevTools の Performance で確認する
- 過去に机上判断で誤った実装を入れた実績がある

### 画面の検証（常設ハーネス）

`docs/index.html` の挙動を変えたら **`python verify_ui.py` を流し、
結果を完了報告に貼る。**

```
python verify_ui.py                 # 全ケースを normal(430×900) / compact(360×690) で
python verify_ui.py --list          # ケースの一覧
python verify_ui.py --case reward   # ケースを絞る（複数指定できる）
python verify_ui.py --keep-going    # 落ちても最後まで回す
```

- **1 つでも落ちたら赤**（終了コード 1）。`make10.py blob` の 15 項目と同じ思想
- Chrome を自動で立ち上げる。見つからない環境では `MAKE10_CHROME` に
  実行ファイルのパスを入れる
- **キャッシュを使わない。** 配信は条件付き要求を無視して常に 200・
  `Cache-Control: no-store`、ブラウザは `Network.setCacheDisabled`、
  Chrome のプロファイルは実行ごとに一時ディレクトリに作って終了時に消す
  （5.4 の検証は、固定パスのプロファイルに残った 5.3 を 304 で読み続けていた）
- **版の照合で止まる。** 起動直後に `docs/index.html` のソースの `APP_VERSION` と、
  設定画面に見えている `#appver` を突き合わせ、食い違えばケースを 1 つも回さず
  赤で終わる（キャッシュ以外の原因で古い版が読まれた場合もここで止まる）
- **入れるのは回帰だけ** ―― 起動と画面の巡回・保存データの移行・3 モードの出題と
  件数・ヒントの消費規則・クリアの確定と挑戦の報酬・JS エラー 0。
  寸法の詰めや縮小率の実測のような**計測ものは入れない**（答えが出たら
  TOKEN_METRICS に焼き付けて終わるもので、毎回回す価値が無い）
- 挙動を変えたら**ケースも同じ作業の中で直す**。仕様書と同じ扱い
- **毎回作り直さない。** BLOB を組むスクリプト・B-4 の 53 項目・5.3 の 84 項目と、
  同じ検証を 3 回書き捨てた。常設にしたのはそのため

## 実装のルール

### 表示文字列は元の木に戻せること

式を文字列にするとき、**標準的な文法で読み直したときに
元の構文木と同じ意味になること。** 値が一致するだけでは不十分。

過去に 2 回この種のバグを出している（GAME-SPEC 12 章）。

### 検証は生成と独立に実装する

生成側の関数を検証に流用しない。同じ前提を共有した検証は検証にならない。

### 寸法の定数は 1 箇所に持つ

トークンや枠の幅などの寸法は **JS 側に定数として持ち、
CSS へは変数で渡す。** CSS と JS に同じ数値を別々に書かない。

### ドラッグ中にレイアウトを起こさない

- 位置や大きさの変化は原則 transform で表現する
- width / top / left のアニメーションはレイアウトを起こす
- 例外的に width を使う箇所（吸着した枠）は、
  **1 要素だけ・短時間に限る**
- 縮小率は事前に算術で求める。offsetWidth を毎フレーム読まない

### 開発者パネルにキーを足したら load() をマージにする

load() は `{...DEV_DEFAULT, ...s.devVars}` の形を保つこと。
そのまま代入すると、古い保存データで新しいキーが undefined になり
レイアウト計算が NaN になる。

### 開発者パネルは削除しない

調整用に使い続けるため。

## 画面のルール

- **画面を作る・変えるときは DESIGN.md を読む。** 配色・文字サイズ・寸法・
  角丸・動き・リスト画面（設定・メニュー）のルールはすべてそちらにある
- 次の 2 点は毎回効くのでここにも残す:
  - **アプリ全体でアクセント色を使わない（モノクロ基調）。** 意味を持つ色は
    コーラル = 注意・エラー、`--slate` = 無効 だけ。`--teal` は開発者パネル専用、
    `--gold` は未使用（詳細は DESIGN.md 1-2）
  - 問題画面は高さ 100dvh。**スクロールを発生させない**
- 配色は藍テーマ 1 つ（白は開発者パネル専用）。色を直接書かず必ずテーマ変数から作る（詳細は DESIGN.md 1 章）

## セッション管理
以下に当てはまったら、作業を続ける前に「ここで区切ることを推奨します」と
理由付きで報告すること（勝手に /clear はしない）。
- 機能が一つ完成して commit した直後
- 同じバグを3回直しても解決しない（文脈が汚染されている可能性）
- 作業対象が変わるとき（問題生成スクリプト → ゲームUI → Capacitor化 など）
- コンテキスト残量が30%を切ったとき
報告時は「/clear と /compact のどちらが適切か」も添えること。

## 変更履歴

| ver | 日付 | 内容 |
|---|---|---|
| 1.4 | 2026-09-06 | devVars の既定値を実測値に更新、APP_VERSION 導入、CLAUDE.md を分割 |
| 1.5 | 2026-09-06 | 比較用の書体切り替えを開発者パネルに一時追加（FONT-TRIAL、決定後に削除） |
| 1.6 | 2026-09-06 | FONT-TRIAL の候補を Zen Kaku 系に入れ替え、数字ウェイト 700/900 切り替えを追加 |
| 1.7 | 2026-09-06 | 書体を Barlow Condensed ＋ Zen Kaku Gothic New（数字ウェイト 700）に確定し FONT-TRIAL を撤去。font-weight:800 は全て 700 に |
| 1.8 | 2026-09-06 | 書体を Zen Kaku Gothic New 単体に変更（伝達ミスの訂正）。Barlow Condensed の link と font-family 参照を削除 |
| 1.9 | 2026-09-06 | 比較用に League Gothic / Oswald を開発者パネルに一時追加（FONT-TRIAL2、ローカル同梱、wdth スライダー付き、決定後に削除） |
| 2.0 | 2026-09-06 | 書体を Zen Kaku Gothic New で確定。FONT-TRIAL2 と assets/fonts/（League Gothic・Oswald）を撤去 |
| 2.1 | 2026-09-06 | 配置候補の統合: 挿入後のトークン列が同一になる枠は最も右の1つだけ残す（既存の括弧の隣に同じ括弧を置くケース） |
| 2.2 | 2026-09-06 | 数字の箱幅 --numW と左右マージン numMx を開発者パネルで調整可能に（TOKEN_METRICS.normal を上書き、compact は比で追従）。数字0〜9の実描画幅と幅の整合チェックを実測値に追加 |
| 2.3 | 2026-09-06 | 開発者パネルの実測表示を退避値方式に変更。設定シートを開くと問題画面が display:none で測れないため、layoutZones()／fit() が画面表示中に測った値を devSnap に退避し、パネルはそれを計測時刻付きで表示するだけにする。未計測時は「未計測」表示 |
| 2.4 | 2026-09-07 | 「幅の整合」の退避を fit() から hideZones() へ移動（fit() はプレイ中ほとんど呼ばれない）。枠を閉じる直前に .expr 実測幅から live 枠の実測幅を引いてトークンだけの幅を退避する |
| 2.5 | 2026-09-07 | 数字の箱幅を確定（TOKEN_METRICS normal num:28/numMx:2、compact num:23/numMx:1）。numW/numMx の調整スライダーと devVars キー、applyMetrics() の上書き処理を撤去。実測表示（描画幅・幅の整合）は検算用に存置。GAME-SPEC 4-5 に「最長式で 85% 縮小がかかるのは仕様」と明記 |
| 2.6 | 2026-09-07 | A群の整理: 挑戦ロック時の効いていない揺れコードを削除／非表示中の階乗表を復元しないよう HIDDEN_UI 定数と factShow(on,persist) を追加（保存値は保持）／制約表示の k===1 死んだ分岐を削除し conOf() に k!==0 警告を追加 |
| 2.7 | 2026-09-07 | 2.6 の積み残し。未使用 CSS .chip .badge を削除（挙動変更はこれだけ）。tabular-nums は Zen Kaku では無効だがフォールバック用に存置する旨のコメントを .eq に追加。仕様書を実態に合わせて修正（GAME-SPEC 3: 制約違反はそもそも組めず判定は保険／GAME-SPEC 5: display:none!important／DESIGN.md 2-1: tabular-nums／CLAUDE.md: パス表記と「ファイルの配置」節）。web/assets/fonts/ の残骸はユーザー側で削除済みを確認 |
| 2.8 | 2026-09-07 | リスト画面の作り直し（試作・LIST-TRIAL）。設定行を .group（節ごとの面）＋インセット区切り線に。.sheet h3 を --slate に、遷移矢印 › を .chev（SVG）に、モードの絵文字を .ic 形式の SVG に統一。#settings/#stats を .group で囲み、.kv を設定行に統一、.th を padding 14px に。ついでに #ctot が scoreOn を無視していたバグ修正、#m-held の .lock を外す。比較用に web/v27.html（2.7）を保存。DESIGN.md 6-3 に試作の記録を追加（6-1/6-2 は未変更） |
| 2.9 | 2026-09-07 | 単色化（試作・COLOR-TRIAL）。--teal / --gold の使用箇所を --paper に統一（開発者パネルの --teal 3 箇所のみ据え置き）。#factbtn.on の直値 rgba を color-mix に。テーマを藍 1 つに絞り sumi/fuji を削除、白は開発者パネル専用として定義存置。未使用の --tileBg/--tileInk/--par を削除。applyTheme() に現存しないテーマ→藍の移行を追加。THEMES を 2 件に縮小し paper のスウォッチ色ずれ（#F2EFE6→#E8E3D5）を修正。比較用に web/v28.html（2.8）を保存。DESIGN.md 6-4 に試作の記録を追加（1 章・6-1・6-2 は未変更） |
| 3.0 | 2026-09-07 | COLOR-TRIAL を確定（DESIGN.md 1 章・1-2 に反映、6-4 削除、6-3 の却下手順を訂正）。明るさの段階を整理: --dim（--paper の 70%、color-mix を変数化）を新設し、リスト画面の副次テキスト（.small / .chev / プレースホルダ / .mode の値）を --slate → --dim に、.sheet h3 を --paper・14px に。--slate は「無効」専用に限定（#play の --slate は不変）。ホーム整理: .tag と m-course/m-free の説明文を撤廃、.mode を「アイコン＋名前＋右端の値（.mv）」の 1 行に。挑戦ロックの文言を「あと N 問」に短縮。#heldpick も同構造（0 でも件数表示）。GAME-SPEC 7-2 更新 |
| 3.1 | 2026-09-07 | バグ 3 件の修正。①非表示中は applyScale() が縮小率に触らないようにし、go("play") で測り直す（設定内のトグルで scale が 0.5 に落ちる／長い式の縮小が外れる／start() の初回描画が非表示のまま走る、をまとめて解消）。②設定のトグルの死んだガード if(SCR==="play")render() を playDirty＋enterPlay() に置換。③「全部消す」を clearAll() に分離し、時間・手数・ヒントを引き継ぐ。④ヒントを見た段階を G.hints として端末に保存（解き直し・再起動でも戻らない）。⑤solved フラグを導入し、record() を advance() から win() へ移して記録を 1 回だけに。正解カードは lastWin から出し直す。GAME-SPEC 3-1・4-3・8-1・9・10 を更新 |
| 3.2 | 2026-09-07 | 「全部消す」が正解カードを隠さないようにした（clearAll() の $("win") 非表示を !solved のときだけに）。正解後に押すと「つぎへ」が消えて先へ進めなくなっていた。記録は正解時に確定済みなので、カードは盤面の内容と独立に出したままでよい。GAME-SPEC 8-1 の該当行を修正 |
| 3.3 | 2026-09-07 | 寸法調整用スライダーの一時追加（SIZE-TRIAL、**決定後に撤去する**）。実機（Android）で数字・階乗・アイコンが小さすぎる件を実機で詰めるため、開発者パネルに 数字/階乗/演算子/括弧の文字サイズと箱幅・トークンの高さ・.iconbtn・#gear の 11 項目を追加。文字サイズと .iconbtn/#gear の寸法を CSS 直値から TOKEN_METRICS へ移し、applyMetrics() が --numF/--opF/--facF/--parF/--btnW/--gearW として流し込む形に統一（@media の文字サイズ 3 行と .iconbtn の上書きを削除）。スライダーは normal だけを動かし、compact は既定値どうしの比で追従（SIZE_KEYS / SIZE_RATIO / metricsFor()）。DEV_DEFAULT へ 11 キーを TOKEN_METRICS.normal 参照で追加、SIZE-TRIAL 専用のリセットボタンと compact 追従値の表示を追加。実測表示（数字の描画幅）もスライダーの値で測るように変更。**既定値は 1 つも変えていない** |
| 3.4 | 2026-09-08 | SIZE-TRIAL の調整環境を直す（**SIZE-TRIAL は決定後に撤去する**）。#devpanel を #settings の中から #app 直下へ移す**恒久的な構造修正**（position:fixed だけでは #settings が display:none のとき描画されないため、DOM 上の場所を出す必要がある）。画面下部に重ねる形にし（高さ 40dvh・パネル内スクロール・sticky ヘッダ）、開く導線は設定シートの #dv-open-row、閉じるのは #dv-close、全体を見渡すための「たたむ」#dv-min を追加。#devpanel は SCREENS に入れないので go() は無変更。renderDevPanel() から .hide の切り替えを外し、devOpen / devPanelShow() に分離。あわせて .plainbtn（メニューボタン）を .iconbtn と同じ --btnW 共有にし、@media の 38px 指定を削除。devSnap（2.3〜2.4）は据え置き |
| 3.5 | 2026-09-08 | アイコンの形の修正。①ICON_STAR のパスが左右非対称で viewBox 下端をはみ出していた（y=4〜23.6、stroke 込み 24.6）ので、中心 (12,12)・外周 8.2・内周 3.13 の左右対称な五芒星に書き直し。同じ壊れたパスが #heldpick の hp-chal にも直書きされていたので両方直した（y=3.8〜18.63 に収まる）。②#gear の絵文字 ⚙ を .ic 形式の SVG（ICON_GEAR、8枚歯）に置換。設定画面での ← は ICON_BACK（.chev を x=12 で左右反転した ‹）にし、go() が innerHTML と title/aria-label を差し替える形に変更。③他のアイコン 12 種はgetBBox で viewBox 内・対称性を確認し、いずれも問題なし（ICON_TROPHY の脚と台座の2.5px の隙間だけ報告済み・未修正）。形の修正のみでサイズ・デザインは変更なし |
| 3.6 | 2026-09-08 | ICON_TROPHY の脚を M12 14v3.5 → M12 14v6 に修正。脚が y=17.5 で終わっていて台座（y=20）との間に 2.5px の隙間ができていた座標の誤り。描画して脚と台座が繋がっていることを確認（脚 y 14〜20 / 台座 y 20、隙間 0）|
| 3.7 | 2026-09-08 | アイコンの描画サイズを SIZE-TRIAL に追加（**決定後に撤去する**）。--btnW は箱の大きさしか変えず、中の SVG は .ic{width:24px} 固定だったので、TOKEN_METRICS に ic:24 を追加し .ic{width:var(--icW)} に。スライダー「アイコンの線の大きさ」（16〜40）を追加。あわせて .ic に flex:0 0 auto を付けた ―― .top button{padding:6px 11px} が .iconbtn{padding:0} に競り勝って #back の内寸が 20px（#menu は 22px）しかなく、24px の .ic が flex で縮んでいた。これが「丸ボタンのスライダーを動かしても三本線が変わらない」の直接の原因。この修正で #back / #menu のアイコンは既定でも 20/22px → 24px になる（#clear / #hint は元から 24px）。.mode .mi は calc(var(--icW) + 6px) にして追従（既定 30px は不変）。.chev は .ic とは別クラス（16px 固定）なので影響なし |
| 3.8 | 2026-09-08 | 入力まわりの修正 4 件。①.tok.swap の塗った角丸タイル（--paper 18%・radius 5）を .chip.sel と同じ「記号を明るくして下線 1 本」に変更。問題画面でトークンに面を塗る箇所が無くなった。②差し替えの当たり判定を距離式に変更: nearZone() を nearest(sel,max) に一般化し nearZone / nearSwap を作成。tgt() は直撃を優先しつつ、外れたときは枠と差し替えを同じ距離で比べて近いほうを採る（同距離は枠）。許容距離 DEV_SWAP を DEV_CATCH とは別キー（devVars.swapCatch、既定 40）にして開発者パネルにスライダーを追加。演算子3つで隙間が無い状態の死に地（数字の上 32px×2）が解消。③式が変わらない差し替え（同じ記号を重ねただけ）で moves を増やさないようにした（ドラッグ・タップ両方）。④タップ配置で held があるときは式の記号を押しても消えないようにした（選択解除はトレイの同じチップ）。GAME-SPEC 4-1・4-3、DESIGN.md 1-2・3-4 を更新 |
| 3.9 | 2026-09-08 | .tok.swap から color:var(--paper) を外し、下線（box-shadow）だけの強調にした。式の中には「数字 → 演算子 → 括弧」の明るさの段階があり、強調中だけ演算子が数字と同じ明るさになるとその段階が一時的に崩れるため。.chip.sel が明るさを上げているのはトレイの中に明るさの段階が無いからで、式の中とは事情が違う。DESIGN.md 1-2 にこの区別を追記 |
| 4.0 | 2026-09-08 | SIZE-TRIAL にトレイと階乗の食い込みを追加（**SIZE-TRIAL は決定後に撤去する**）。①開発者パネルを上側／下側に付け替える #dv-pos を追加（devVars.devTop に保存）。下だとトレイが必ず隠れて調整できなかった。クラス名は .dvtop ―― .top にすると問題画面のヘッダ .top{display:flex} に当たってパネル自体が横並びの flex になる。ヘッダはボタン 3 つで折り返すので見出し側を省略表示に。②TOKEN_METRICS に chipF（.chip の文字サイズ 33/26）・trayGap（.tray の gap 2/2）・trayPad（.tray の padding 6/4）を追加し、CSS は var(--chipF)/var(--trayGap)/var(--trayPad) 受けに。@media の .chip{font-size:26px} と .tray{padding:4px} は二重管理になるので削除。③定数 FAC_ML(-2) を廃止し TOKEN_METRICS.facMl（normal/compact とも -2、比 1.0）へ移動。tokensWidth() も MET.facMl を見る。④SIZE-TRIAL が 16 項目になったので「式のトークン／トレイ／ボタン・アイコン」の 3 節に分割。#devpanel h3 に .sheet h3 と同じ様式を指定（3.4 で #settings の外へ出して以来効いていなかった）。**既定値は 1 つも変えていない** |
| 4.0 追補 | 2026-09-08 | DESIGN.md の訂正のみ（index.html は無変更・APP_VERSION 据え置き）。6-5 を 3.4 の #app 直下移動と 4.0 の .dvtop/.min/#devpanel h3 に合わせて全面改稿、3-1 の FAC_ML 行を facMl へ、2-2 の compact の説明を訂正 |
| 4.1 | 2026-09-08 | SIZE-TRIAL の normal を確定し、compact も実機で決められるようにした（**SIZE-TRIAL 自体はまだ撤去しない。撤去は次の作業**）。①TOKEN_METRICS.normal を実機の確定値へ（num32/op30/fac19/par16/h59/facMl-3/numF52/opF32/facF39/parF42/btn52/gear42/ic28/chipF38/trayGap0/trayPad9。numMx はスライダーが無いので 2 のまま）。compact は未確定のまま据え置き。②SIZE_RATIO（compact を比で追従させる仕組み）を廃止し、devVars を sizeN / sizeC の 2 組に分割。スライダーは matchMedia(COMPACT_MQ) で判定した「今の画面のモード」を直接編集し、モードが切り替わったら matchMedia の change で renderDevPanel() を呼び直す。sizeN/sizeC は入れ子なので {...DEV_DEFAULT} の浅いコピーだと既定値を共有して壊れる ―― devDefaults() を追加。3.3〜4.0 のフラットな保存データは load() で SIZE_KEYS を捨てて既定から作り直す。#dv-copyline を dev / normal / compact の 3 行（textarea）に。#dv-reset・#dv-size-reset は 2 組とも既定へ。compact の既定値がスライダーの下限を割っていた（trayPad 4<6 / btn 38<40）ので可動域の min を広げた。③開発者パネルの節を h3 クリックで折りたためるように（▾/▸、G.devSecs に保存、既定は SIZE-TRIAL の 3 節だけ開）。見出しの無かった先頭の塊に「枠・吸着（基本）」を与え、SIZE-TRIAL の帯は節ではないので折りたたみ対象外。#devpanel.min（パネル全体をたたむ）とは別物。④:root の --dur/--hotDur を .14s/.1s → .3s/.2s にして applyDevVars() の上書き値と揃えた（挙動は不変）。DESIGN.md 2-2・3-1・6-5・7 章、GAME-SPEC 4-5 を更新 |
| 4.2 | 2026-09-08 | 4.1 で書いた**誤った実測値の訂正**と、置き場所を誤っていた UI の削除。①DESIGN.md 3-1 と GAME-SPEC 4-5 に例として書いた最長の式 `(((( 5!!+8!!×3!!+6!! ))))` は**このゲームでは作れない**（canPut() は `!` の左に `!` を置けない。GAME-SPEC 2-4）。原因は 4.1 の計測スクリプトの出力行 `GLYPH[t.t]` ―― GLYPH は種類キー（"+" "!" "lp"）で引く表で t.t（"op" "fac"）では引けず、演算子と階乗が undefined になって join("") で消えた。残った `((((5836))))` から内訳（階乗 8・数字 4）を見て手で復元したのが誤り。**縮小率 79%/88% 自体は livePositions()＋put() で組んだ合法な状態から出ており正しかった。**②canPut() だけを辿る全探索（6,890,240 状態）で最長の式を求め直し、23 トークン（数字4・演算子3・( 4・) 4・! 8＝各記号の上限）・幅 490px(normal)/443px(compact)、実測 79.2%/88.5% を確認。例の式を `((((5!)!)!)!)!+8!+3!+6!` に差し替え、導出（上限の根拠と式）も併記。③#dv-compact-v を削除（4.0 までは比で算出した compact 値を見る唯一の窓だったが、4.1 で比を廃止して役目が終わった。同じ内容は #dv-copyline の 3 行目が出す）。renderDevMeasured() の代入も削除。空の `<b>` ごと消えた。④GAME-SPEC 4-5 の開発者パネルの記述を現状に（スライダー 6 本→23 本の内訳、SIZE-TRIAL は決定後に撤去・normal 確定/compact 未確定、節の折りたたみ、`#app` 直下で全画面に重なること、2.5 で撤去→3.3 で作り直したという順序）。⑤DESIGN.md 3-5 の参照先「6 章の不揃い」を 7 章に訂正 |
| 4.2 追補 | 2026-09-08 | 文書の訂正のみ（index.html は無変更・**APP_VERSION は 4.2 で据え置き**）。①AGENTS.md の削除を指示されたが、**リポジトリにも Git の索引にも全履歴にも存在しなかった**（find・git ls-files・git log --all で確認）。作った記録も無いので削除はしていない。②CLAUDE.md「バージョン管理」に運用ルールを 2 つ追加 ―― 完了報告に git status も貼る（git diff --stat は新規の未追跡ファイルを出さないので、それだけでは増えたファイルを見落とす）／指示されていないファイルを作らない・消さない・改名しない（必要なら実行せず提案として報告する）。③GAME-SPEC 4-5 末尾の「パネルを開くと問題画面が display:none になる」を訂正。3.4 以降 #devpanel は #app 直下で SCREENS に入らず、#dv-open が goBack() で元の画面へ戻すため、パネルを開いているあいだ問題画面は見えている。ただし設定シートを開き直すと go("settings") が #play を隠すので測れる場面と測れない場面が混ざる ―― devSnap（退避方式）の説明はその理由に付け替えて存置。④DESIGN.md 2-1 の「11 章」に文書名を補って「GAME-SPEC 11 章」に（DESIGN.md は 7 章までしかなく誤読しうる）。文書名なしの他文書参照は grep で全数確認し、この 1 件だけだった。⑤縮小率 79.2%/88.5% に測定条件（画面幅 430px）を明記し、幅ごとの実測表（430/390/360px）を DESIGN.md 3-1 に追加。狭い端末ほど縮むことを両文書に明記 |
| 4.3 | 2026-09-09 | **SIZE-TRIAL の撤去。** 両モードの寸法が実測で確定したので、値を TOKEN_METRICS に焼き付けて調整の仕掛けを丸ごと消した。①確定値: normal（実機 Android）num32/numMx2/op30/fac19/par16/h59/facMl-3/numF52/opF32/facF39/parF42/btn52/gear42/ic28/chipF38/trayGap4/trayPad9、compact（DevTools 412×690）num29/numMx1/op24/fac17/par12/h50/facMl-3/numF44/opF29/facF34/parF36/btn44/gear38/ic26/chipF33/trayGap4/trayPad8。numMx だけはスライダーが無かった項目で 2.5 の値を引き継ぐ。②撤去したもの: スライダー 16 本（23→7 本）とその dv-*-v 表示、節 tok/tray/btn、SIZE-TRIAL の帯の見出しと #dv-mode、#dv-size-reset、SIZE_KEYS、sizeMode()、sizeDefault()、DEV_DEFAULT.sizeN/sizeC、applySizes()、devSizeInput()、matchMedia(COMPACT_MQ) の change 購読、コメント中の SIZE-TRIAL の記述。③畳んだもの: metricsFor() は devVars の重ね合わせが消えて TOKEN_METRICS の複製を返すだけになったので applyMetrics() に畳んだ。devDefaults() は入れ子が消えたので {...DEV_DEFAULT} に戻した（load() が CLAUDE.md 指定の形に戻る）。#dv-copyline は 3 行の textarea から 1 行の input へ。④残したもの: applyMetrics()/MET（CSS 変数へ流す唯一の経路。tokensWidth() も MET を見る）、モード切り替え（担っているのは addEventListener("resize") で、消した matchMedia 購読は renderDevPanel() を呼ぶだけだった）、節の折りたたみ、実測値の節、枠・吸着の 7 本。⑤節の既定を basic+meas 開・copy 閉に変更（スライダーと実測値は 1 往復で使うため。画面高 690px で basic のみ 636px / basic+meas 964px、どちらもスクロールは要る）。⑥保存データの移行: DEV_DEFAULT に無いキー（旧フラット寸法・sizeN/sizeC）と DEV_SEC_DEFAULT に無い節キー（tok/tray/btn）を load() で捨てる。⑦検証: 4 通り（430/360px × normal/compact）で CSS 変数 17 項目一致・最長式 ((((5!)!)!)!)!+8!+3!+6! が canPut() を通ることを毎回確認・幅の整合の差 0。縮小率は normal 79.2%/64.9%、compact 97.0%/79.7%（430/360px）。数字の片側余白は normal 3.0px / compact 3.5px でマイナスなし。JS エラー 0。DESIGN.md 2-2・3-1・6-5・7 章、GAME-SPEC 4-5 を更新 |
| 4.4 | 2026-09-09 | 括弧の配置候補の統合を「外側へ育つ」形に変更。統合された候補（挿入後のトークン列が同一になるもの）の勝ち残りを、従来の一律「最も右」から**開きは最も左・閉じは最も右**に変えた。①記号ごとの表 NEST={lp:"open",rp:"close"} を GLYPH / TRAY の隣に新設し、livePositions() は NEST[kind] を引くだけにした（記号名での分岐はしない）。②livePositions() の「昇順ループの後勝ちで右が残る」という順序への暗黙の依存をやめ、keepLeft の比較で残す側を明示。③変わるのはドラッグ中に隙間が開く位置と指を離す場所だけで、**置いた後の式は変わらない**（統合された候補は挿入後の列が同一）。実際に ) は従来から外側だったので、動きが変わるのは ( のみ。④検証: 到達可能な 400,000 状態 × 8 記号を走査し、候補の要素数のハッシュ（9caf68e1）と「各候補に置いた後のトークン列の集合」のハッシュ（98d96703）が変更前後で完全一致、位置のハッシュだけが変化（474a6a5d→a23d1043）。統合 252,034 グループの勝ち残りは max 252,034 → lp が min 6,271 / rp が max 245,763 に。統合が起きるのは lp / rp だけで他の記号では 0 件（canPut が同記号の隣接を許さないため）。枠が並びの左端／右端に開くこと、先頭 (( での位置 0、在庫 4 使い切りで枠が出ないこと、タップ配置でも同じ枠、最長式 ((((5!)!)!)!)!+8!+3!+6! が枠だけを辿って 19 手で組めることを実測。JS エラー 0。GAME-SPEC 4-2 を更新（12 章は該当なし・DESIGN.md も該当なし）|
| 4.5 | 2026-09-09 | 4 件。①**歯車を廃して三本線に統一。** 同じ「設定を開く」にホームは歯車・問題画面は三本線と 2 つの見た目があった。#menu の直書き SVG を定数 ICON_MENU に寄せ（他のアイコンが既に定数なのに揃える／片方の要素をもう片方の正にしない）、#menu へは初期化時に、#gear へは go() が同じ文字列を流し込む。ICON_GEAR は削除。#gear の title/aria-label は「設定」のまま、ホーム以外での ICON_BACK 切り替えも維持。②**アイコンの線を細く。** .ic の stroke-width を 2 → 1.5。三本線だけ 2 を保つため .ic.thick を新設し、その class を ICON_MENU の中に書いた ―― 太さを置き場所ではなくアイコンに紐づけたので #menu / #gear のどちらでも 2 になる。normal/compact では分けない。塗りのアイコンは 1 つも無く（全 13 個が fill:none）、全部が細くなることを実測で確認。.chev は別クラスなので 2 のまま。③**問題番号を x/1000 形式に。** 本編「本編 12/1000」＝位置/全体、挑戦「挑戦 34/2100」＝出題数/全体、フリー・保留・共有は空。分母は COURSE.length / CHAL.length から取る。散っていた #pmode の書き込み 4 箇所を loadPuzzle() の 1 箇所に集約（advance() 経由では番号が更新されていなかった不具合も同時に解消）。④**挑戦モードの重複出題を解消。** G.chalShown（出題済みコード）を追加し、抽選は chalDone（クリア済み）ではなくこちらを除外する。積むのは loadPuzzle() の時点＝表示した以上は出題済み。解かずに送る・保留する・ホームへ戻る、のどの経路でも二度出ない。保留した問題が保留リストと挑戦の抽選に二重在籍する状態も解消。既存データは chalShown が無ければ chalDone で初期化。打ち止めの文言を「全問クリア済みです」→「全問出題しました（クリア N/2100）」に（出題済みで数えるので未クリアでも打ち止めになりうる）。ホーム・統計の N/2100 はクリア数のまま。⑤検証: #menu と #gear の innerHTML が完全一致・ともに 2px、他 11 アイコンが 1.5px・塗り 0 個、設定画面で ‹ になり goBack() が動くこと、5 モード×全経路（開始/つぎへ/べつの問題/保留/共有コード）の #pmode、挑戦で 60 回連続の「送る・保留・入り直し」で重複 0・二重在籍 0、旧データからの移行でクリア済み 3 問が再出題候補に残らないこと、最大桁（本編 999/1000・挑戦 2100/2100）で .info が溢れず左右のボタンにも食い込まないこと（430×900 / 430×690 / 360×690）。JS エラー 0。GAME-SPEC 5-3（新設）・7・7-3・9、DESIGN.md 3-1-2（新設）・6-3・6-4 を更新 |
| 4.6 | 2026-09-09 | **4.5 の未達分の修正。** 4.5 で #pmode に「本編 12/1000」を入れたが、親の .top .info が CSS の #rule,.top .info,.trayrow,#hold,#alt{display:none!important} に含まれたままで、**画面には出ていなかった**（実測で display:none・高さ 0）。①その並びから .top .info だけを外した（#rule / .trayrow / #hold / #alt はモックアップのまま据え置き）。②HIDDEN_UI から info を外し、コメント中の CSS の列挙も実態に合わせた（列挙が古いままだと CSS と定数の対応を誤解するため）。なお HIDDEN_UI.info は**どこからも読まれていなかった**（読まれているのは factTable のみ）ので、外しても機能への影響はない。③#pcode（コード・難易度・正解数）は .info の子なので同時に見えるようになる。毎問必ず中身が入るので空行にはならない。④検証: 本編「本編 12/1000」・挑戦「挑戦 2100/2100」が display:block・高さ 32.4px で**実際に見えている**こと、フリー/保留/共有で #pmode が空になること、空でも .top の高さ（normal 52px / compact 44px）と #exprwrap の位置（454 / 331）が**非表示だったときと 1px も変わらない**こと、最大桁でも #back / #menu に食い込まず #pmode が折り返さないこと（430/360px × normal/compact）、問題画面にスクロールが出ないこと、JS エラー 0 を実測。幅 360px 以下では #pcode が 2 行に折り返して .info が 48.6px になるが、丸ボタン 52px の中に収まるので .top の高さは変わらない。**表示位置は右寄せ**（.info{flex:1;text-align:right}）で中央ではない ―― 位置を変えるかは別途判断。GAME-SPEC 5 章冒頭・5-3 を更新（11 章に .info の記述は無く該当なし／DESIGN.md も該当なし）|
| 4.7 | 2026-09-09 | **ヒントを消費資源にし、操作を作り直した**（保留の廃止と挑戦モードの報酬は次の作業）。①残数 G.hintStock を追加（初期値 HINT_START=100・テスト期間の暫定値）。名前は「消費される残り個数」を既に stock と呼んでいる先例（PAREN_STOCK / .chip .stock）に合わせた。問題ごとの到達段階 G.hints とは別項目。項目が無い保存データは 100 で始め、進捗リセットでも 100 に戻る。②消費するのは hintLv > G.hints[code]（その問題の到達済み最大）の段階を初めて開いたときだけ。見返し・閉じて開き直し・解き直しはすべて無料。残数 0 では新しい段階を開けず、#sub に「ヒントの残りがありません」と赤字で出す（黙って無反応だと壊れて見えるため。保留の上限と同じ見せ方）。**減点 SC.hint は変更なし**（減点＝問題ごと・消費＝アカウント全体で併存）。③#hint は「次の段階へ」だけを担い、3 で止まる（0 へ回る挙動を廃止）。「‹ 前へ」「閉じる」と段階表示「N / 3」を #hintbox の中（.hintbar）へ。段階 1 では前へを disabled に。閉じても hintMax は下げない。#hintlabel は**目に見えない読み上げ用ラベル**（.btnlabel）だったので削らず、段階と残数を言葉で持たせる役に変えた（見える数字は aria-hidden）。④丸ボタンの右肩に残数を重ね、数字にかかる部分の線を削る。線は border のままだと数字を貫くので #hint::before に移して CSS の mask で穴を開けた（塗って隠す案は地がグラデ＋模様で当て板が見えるため却下、SVG 化案は他の丸ボタンと線の見た目が揃わなくなるため却下）。箱は 22×15 固定・文字 11px 直値で TOKEN_METRICS には足さない（あそこは実機で確定した寸法の表。.chip .stock も直値 12px で揃う）。1000 以上は 1k（1500 も 1k）、0 でも出す。統計「ヒントの残り」は丸めず実数。⑤検証: 3 段階で 3 消費、3 で押しても無反応、前へ/次への往復と閉じ開き・解き直しがすべて無料、別問題では改めて消費、残数 0 で新規不可・見返し可、到達 2 の状態で 1〜2 無料・3 で 1 消費。0/99/100/999/1000/1500 を描画し、穴の半径 13.5px に対し文字の角までの余裕が最小 1.19px で**線が数字を貫かないこと**を実測（12.5px では余裕 0.19px しかなく広げた）。430/360px × normal/compact ではみ出し・スクロールなし、ヒントボックスはトレイを押し下げない。保存→再読み込みで残数と到達段階が保たれること、旧データ（項目なし）が 100 で始まること、減点が従来どおり（SC.hint[3]=0.3 / k=0.536 / 32 点）であることを確認。JS エラー 0。GAME-SPEC 3 章・5-4（新設）・9 章・10 章、DESIGN.md 3-1-3（新設）を更新 |
| 4.7 追補 | 2026-09-09 | **AGENTS.md の再発と削除**（index.html は無変更・**APP_VERSION は 4.7 で据え置き**）。①再発は 2 回目（1 回目 9/8 22:47:33、今回 9/9 11:04:55。いずれも JST）。中身は CLAUDE.md（4.7 時点・38,667 バイト / 173 行）の完全な複製で、違いは本文中の "CLAUDE.md" を "AGENTS.md" に置換した 4 箇所だけ（138 行・151 行・168 行・169 行）。CLAUDE.md 内の "CLAUDE.md" は 4 箇所しかなく、その**全部**が置換されていた。②**138 行と 151 行の置換内容は事実に反する** ―― 1.4 で分割したのも 2.7 で直したのも CLAUDE.md であって AGENTS.md ではない。機械的な文字列置換なので変更履歴の記述が誤りに変わる。CLAUDE.md 側は正しいので**書き換えていない**。③**原因は特定できた: Codex デスクトップアプリの「外部エージェント設定の取り込み」**（app-server の `externalAgentConfig/detect` → `externalAgentConfig/import`、provider_id = "claude-code"）。`~/.codex/state_5.sqlite` の `external_agent_config_imports` に `item_type=AGENTS_MD, source=CLAUDE.md, target=AGENTS.md` が 2 件（完了時刻 9/8 22:47:33.938 と 9/9 11:04:55.359）記録されており、AGENTS.md の作成時刻 11:04:55.229 と一致する。`~/.codex/logs_2.sqlite` にも同時刻の import 要求が残っている。設定は `~/.codex/config.toml` の `[desktop] external-agent-import-sync-enabled = true` ／ `external-agent-import-sync-item-types = "all"`。Codex アプリを起動するたびに走り、AGENTS.md が無ければ作り直す（9/8 に手で消した後、次の起動 9/9 11:04 で再生成された。9/9 16:17 の起動時は既に在ったので import は走っていない）。**Claude Code 側には原因は無い** ―― Claude Code のセッションログ・プロンプト履歴とも 11:04 前後は無活動（直前の記録は 8:30、次は 16:37）。④**分からなかったこと**: この取り込みがユーザーの明示操作を経たのか完全自動なのかは、ログからは判別できなかった。⑤**.gitignore には追加していない。** 未追跡ファイルとして `git status` に出るままにしておく ―― 見えなくすると次に作られたときに気づけないため。⑥再発防止（Codex 側の設定変更）は**実行せず提案のみ**とした |
| 4.8 | 2026-09-09 | **ヒントボックスを横 1 行にし、操作を記号だけにした。** `‹ 1 / 3 本文 › ×`。①2 段構成（本文の下に `.hintbar`）をやめ `.hintrow` の 1 行に。`‹` `›` `×` は記号のみで文字を出さない（意味は位置と `aria-label` で持たせる）。段階表示は `1 / 3` で**括弧を付けない**（`(` `)` が式の記号として出てくるので紛らわしい）。伸び縮みするのは本文だけ（`flex:1 1 auto`・`min-width:0`）で、**左右の操作は本文の長さによらず 1px も動かない**（短い本文と最長の本文で `‹` `›` `×` の `left` が完全一致することを実測）。②**`›`（到達済みの範囲を進む）を新設。** `hintLv < hintReached()` のときだけ有効・**消費しない**。無効時は `‹` と同じく薄くして押せなくする（消さない ―― 位置が動くため）。③**丸ボタン `#hint` の役割を「未到達の段階へ進む」に変更。** 行き先を `hintLv+1` から `hintNextLv()=min(3,max(hintLv,hintReached())+1)` へ。到達 2・表示 1 で押すと **2 ではなく 3 へ飛ぶ**（2 を見返すのは `›` の仕事）。消費の判定は `next > hintReached()` のまま**変えていない** ―― 式はそのままで `next` の求め方だけが変わった。到達 3 で閉じている状態から押すと `min` で 3 に丸められ `3 > 3` が偽になるので**無料で 3 を開き直す**（指示の「閉じている状態から押したときは到達済みなら無料でその次へ」と一致）。④文面を短縮: `使う記号：` / `形：` / `解答例：`。**「括弧は使いません」という否定文を廃止**し、括弧を使う問題だけ記号の列の末尾に `(` `)` を混ぜる。記号の並びは `OPSET()` の出現順ではなく**トレイと同じ `TRAY` 順**に揃えた（`hintSymbols()` を新設）。`<code>` の強調は**残した** ―― 1 行に詰めた結果、ラベルと中身を分ける手がかりがこれだけになったため（幅の増加は +0.6px で無視できる）。`#hintlabel`（読み上げ専用）は「ヒント N/3 を表示中。次は M/3。残り K」に。⑤寸法は JS の `HINT_ROW={btnW:32,btnH:48,gap:2}` に持ち `applyMetrics()` が `--hintBtnW`/`--hintBtnH`/`--hintGap` として流す（CSS に直値を書かない）。`TOKEN_METRICS` には足さない ―― あちらは実機で確定させた表で、こちらは「幅 360px に収まるか」という別の制約から出た値（`.stocknum` と同じ扱い）。`.hintbox` の `padding` を `9px 12px` → `4px 2px`（本文の取り分を 12px 稼ぐため。ボタンが地も枠も持たないので見た目の余白はボタンの内側で足りる）。⑥**タップ領域は 32×48px。** 縦は推奨の 48px を満たすが**横は満たさない**。幅 360px で解答例を 1 行に収める上限がここだからで、48px にすると 2,337 問（11.0%）が入らない。34px は溢れ 0 問だが余裕 0.8px しかなく採らなかった（32px は 6.8px）。DESIGN.md 7 章の「48px 未満」に記録した。⑦検証: **全 21,238 問**の段階 3 本文を実測し最大 195.2px（`解答例：( 3! )! ÷ ( 9! ÷ ( 8 − 0! )! )`）、幅 360px の `compact` で本文に使えるのは 202.0px、**余裕 6.8px・溢れる問題 0 問**（幅 430px では 272.0px・余裕 76.8px）。挙動は 41 項目を 360×690 と 430×900 の両方で確認し**両モードとも 41/41 合格・JS エラー 0**（段階 1 で `‹` が無効／到達段階で `›` が無効／到達 2・表示 1 で `›` は 2 へ無料・丸ボタンは 3 へ 1 消費／到達 3 で閉じて開き直し `›` で 1→2→3 が全部無料／残数 0 で未到達は不可・見返しは可／括弧あり `使う記号：− ( )`・括弧なしで否定文なし／`SC.hint` が `[1,0.75,0.5,0.3]` のまま）。箱の高さは 88.2px → 58px に下がり、`.overlays` は `position:absolute` なので**トレイと式の位置は 1px も動かない**。問題画面にスクロールなし。**保留（`#hold` / `G.heldC` / `G.heldT` / `G.heldF`）と `#pcode` には触っていない。** GAME-SPEC 5-4 を全面改稿（丸ボタンと `›` の役割の違いを明記）、DESIGN.md 3-1-4 を新設・3-5 と 7 章に追記 |
| 4.9 | 2026-09-09 | **4.8 のヒント行の見た目を 3 点修正**（挙動・消費の判定には一切触っていない）。①**記号の上下ずれ。** 原因は字形で、`.hbtn` の `align-items:center` は正しく効いていた ―― 中央に置かれるのは行ボックスで、字の「インク」ではない。ピクセル走査で実測すると、行の中心を 0 として `‹` `›` が **−4.87px**、`×` が **−6.00px**、本文が +0.25px。つまり `‹ ›` は `×` より 1.13px 低く、**3 つとも本文より 5〜6px 高い**位置に描かれていた（どれも baseline より上にしか墨が乗らない字形のため）。**直し方は SVG 化を選んだ** ―― 設定画面の `.chev` が「文字の `›` は書体で位置・太さが揃わないのでインライン SVG に」と同じ結論を既に出しており、それに倣うのが一貫するため。字を替える案は baseline 基準である限り本文との段差が残り書体を替えると再発、位置の微調整案は書体が変わるたびに合わせ直しになるので採らなかった。**3 つとも** SVG にした（1 つでも文字を混ぜると別の不揃いになる）。`ICON_HPREV`/`ICON_HNEXT`/`ICON_HCLOSE` を追加し、いずれも viewBox の中心 (12,12) に上下対称（`‹`/`›` は y 6〜18、`×` は 7〜17）。`‹` は `ICON_BACK` と同じ形なので**パス文字列を `CHEV_L`/`CHEV_R` に括り出し、`ICON_BACK` もそこから作る**ようにした（同じ形を 2 箇所に書かない。4.5 と同じ考え方）。大きさは `HINT_ROW.icW=20`（`--hintIcW`）で、`.ic` の `stroke-width:1.5` は viewBox 座標なので描かれる線は 1.25px ―― `.chev` の 1.33px とほぼ同じ。**結果は 3 つとも行の中心ちょうど（±0.00px）**、5 要素のばらつきは最大 0.88px（4.8 は 6.88px）。②**本文を中央寄せ。** `.hbody` を `display:flex` ＋ **`justify-content:safe center`** にし、中身を `.hbin`（`flex:0 0 auto`・`nowrap`）で包んだ。`safe` が要る ―― ただの `center` だとあふれたときに中身が左右へはみ出し、**左側がスクロールでたどり着けなくなって解答例の頭が読めない**。`safe center` はあふれたら中央寄せをやめて左端から見せる（わざと 3 倍の長さを入れて左余白 0・先頭に到達できることを実測）。左右の余白は 4.25px / 4.27px。③**本文の色。** `.hintbox{color:var(--slate)}` は**誤りだった** ―― `--slate` は「無効・効いていない」専用で、読ませるテキストに使ってはいけない（DESIGN.md 1-1 の表・1-2）。`--paper` に直し、`.hintbox code` からは `color` を外して箱から継承させた（同じ色を 2 箇所に書かない。差は太さ 700 だけ）。これで本文・`<code>`・`‹ › ×` の線がすべて `rgb(245,242,233)` で揃う。**段階表示 `.hlv` は `--dim` のまま据え置いた** ―― ここは「ヒントの中身」ではなく「3 つのうち今どこか」を示す副次テキストで、DESIGN.md 1-1 が `--dim` に割り当てている用途そのもの。本文と同じ明るさにすると行が 1 段に潰れて「中身」と「現在位置」の区別が消える。`--dim` は `--paper` の 70% なので沈む方向で、浮く心配はない。無効な `.hbtn` の `--slate` は正しい用法なので不変。④検証: 挙動 41 項目 × 2 モードで **41/41 合格**（段階 1 で `‹` 無効／到達段階で `›` 無効／`›` は消費しない／丸ボタンは未到達へ進んで消費／残数 0 の警告と見返し無料／`hintMax` と `SC.hint`＝`[1,0.75,0.5,0.3]` 不変）。レイアウト 17 項目 × 2 モードで **17/17 合格**（**全 21,238 問**が 1 行に収まり溢れ 0 問・余裕 6.8px（360px）/76.8px（430px）、短い本文と最長の本文で `‹ › ×` の `left` が完全一致、段階 1/2/3 で行 48px・箱 58px が一定、トレイ不動、スクロールなし）。`ICON_BACK` の括り出しについても 5 項目を確認（設定画面で `‹` になり `goBack()` で三本線に戻る、`.chev` 3 個が無事、`#menu` と `#gear` が一致）。JS エラー 0。**保留（`#hold` / `G.heldC` / `G.heldT` / `G.heldF`）と `#pcode` には触っていない。** DESIGN.md 3-1-4 に「記号は文字ではなく SVG」「本文は中央寄せ」「色」の 3 節を追加し様式表を更新、1-1 の表の `--dim` の用途に `.hlv` を追記、1-2 に `--slate` を読ませるテキストに使わない旨と今回破っていた記録を追加。GAME-SPEC は挙動が変わらないため該当なし |
| 5.0 | 2026-09-10 | **保留の全廃と、挑戦モードの報酬。** 桁を上げたのは保留の削除が大きいため。①**保留を廃止した理由**: 「後で解く」を用意すると、詰まった問題をヒントで解かずに積んで逃げられてしまい、4.7 でヒントを消費資源にした意味が薄れる。ヒントを使うか自力で解くかの選択に絞る。②**削除したもの**（実装から grep して数え上げた 25 箇所）: CSS の `#hold`（`#rule,.trayrow,#hold,#alt` の並びから外す）／ホームの `#m-held` 行と `#m-held-v`／問題画面の `#hold` ボタン／遊び方の「保留…30問まで」の項／`#heldpick` 画面まるごと（`hp-course`/`hp-chal`/`hp-free` の 3 行）／`G.heldC`・`G.heldT`・`G.heldF` の既定値と `load()` の移行 3 行／`nextOf()` の `held`・`heldt`・`heldf` の 3 分岐／`advance()` の同 3 分岐／`SCREENS` の `"heldpick"`／`go()` の `renderHeldPick()` 分岐／`goBack()` の `heldpick` 分岐／`renderHeldPick()`／`$("m-held").onclick`／`$("hp-*").onclick` 3 本／`heldArr()`・`heldMax()`・`isHeldMode()`／`$("hold").onclick`（上限 20/50 の警告を含む。**遊び方の「30問まで」は実装の 20/50 と食い違っていた**）／`HIDDEN_UI.hold`／統計の「本編の保留」「挑戦の保留」「フリーの保留」3 行／`$("reset")` の 3 本／保留に触れたコメント 4 箇所。③**残したもの**（1 つずつ確認済み）: `chalShown`（4.5 の重複防止。保留とは別）／`#alt`（べつの問題。`HIDDEN_UI` で隠れているが保留ではない）／`#rule`／`.trayrow`／`HIDDEN_UI.rule`・`.alt`・`.factTable`／`#pcode`（保留中の案件なので不変）／**タップ配置の変数 `held`**（持っている記号のことで保留とは無関係。`let S=null,tapMode=false,held=null` 以下 12 箇所）。④**`G.ci` の意味が単純になった。** 保留は本編の位置だけ進める経路だったので、`renderHome()` と統計が `G.ci - G.heldC.length` で「保留中の分を引く」計算をしていた。保留が無くなり `G.ci` はクリアでしか進まないので、そのまま使う形にした。**`#alt`（べつの問題）は無影響** ―― `G.swap[G.ci]` に差し替えを記録するだけで `G.ci` を進めないため、保留とは別の経路だった（実測で確認）。⑤**報酬**: `CHAL_REWARD_N=2`。挑戦を 2 問クリアするごとにヒント 1 個、上限なし。計上は `win()`（`record()` と同じ「クリアが確定する 1 点」。`solved` ガードで 1 正解 1 回）。**`advance()` ではなく `win()` に置いた**のは、「つぎへ」を押さずホームへ戻ってもクリアだから。挑戦の問題は `chalShown` により二度と出題されないので同じ問題では稼げない。⑥**端数と「導入時点」の持ち方**: 新項目は `G.chalRem`（0 か 1）**1 つだけ**。導入時点の基準を別に保存する必要はない ―― **項目を持たない保存データを 0 で始めることが、そのまま「ここから数える」を意味する**（既存の 10 問クリアは `chalRem` に入っていないので換算されない）。`chalDone.length` から毎回計算する形は採らなかった（既存プレイヤーに一度に配ってしまう）。名前は `chalDone` / `chalShown` の並びに合わせた。⑦**保存データの移行**: `load()` は**キーごとに明示代入する作り**なので、保存 JSON に残った `held` / `heldC` / `heldT` / `heldF` は `G` に入らず、次の `save()` で JSON からも消える。`devVars` の `k in DEV_DEFAULT` のような選別処理は**要らない**（あれは丸ごと spread しているから必要な処置で、ここは列挙して捨てる処理自体が不要）。⑧**知らせ方はトースト**（「ヒントを 1 個もらいました（残り N）」）を正解カードの 520ms より後の 900ms に出す。正解カードの詳細行 `#wmeta` は「点数を表示する」設定のときしか中身を出さないので、既定（`scoreOn=false`）のプレイヤーに伝わらないため採らなかった。あわせて `renderHintStock()` で丸ボタンのバッジをその場で追従させる。⑨検証（すべて 360×690 と 430×900 の両方・JS エラー 0）: 削除と巡回 **29/29**（`#heldpick`/`#m-held`/`#hold` が DOM に無い、`SCREENS` から消えた、`G` に held 系が無い、残すものが全部ある、ホームが 3 行、スクロール破綻なし、設定→遊び方→戻る→統計→戻る→戻るの巡回）。古い保存データ（`held`/`heldC`/`heldT`/`heldF` と `chalDone` 10 件入り）を仕込んで **15/15**（JS エラーなく起動、`ci=12` を読める、保留キーが `G` にも保存 JSON にも入らない、`chalRem` が 0 で始まる、**いきなり増えない**、11 問目で増えず 12 問目で初めて 1 個、端数 1 で保存→読み直し→次の 1 問で 1 個）。報酬の規則（1 問目×→2 問目○→3 問目×→4 問目○、出題だけでは増えない、本編・フリーでは増えない、トーストとバッジ）。`#alt` と `G.ci` **13/13**（本編で差し替わり `G.ci` は進まず `G.swap` に載る、フリー・挑戦でも差し替わる、`#alt` では報酬が増えない、「つぎへ」でだけ `G.ci` が進む、ホームの表示が `G.ci` と一致）。4.8/4.9 のヒント行の回帰 **41/41 と 17/17**。GAME-SPEC 3 章・5 章（`.info` の並びと `HIDDEN_UI`）・5-3（保留モードの段落を削除）・5-4（**入手経路の表を新設**）・7 章（表から保留の行を削除し廃止の理由を明記、挑戦の行に報酬を追記）・**7-3 を「保留」から「挑戦モードの報酬」に差し替え**・9 章（保留リストを削除し `chalRem` を追加）・10 章（リワード広告から保留枠を削除）を更新。DESIGN.md 6-1（対象画面から `#heldpick`）・6-4（見出し・`.modes` の行数・`.mode.lock`・`.mv` の説明）を更新 |
| 5.1 | 2026-09-10 | **ナビゲーションを「左上＝戻る／右上＝設定」に分け、ホームの三本線の枠を外した。** ①5.0 までは**右上の 1 つのボタン `#gear` が 2 役を兼ねて**いた（ホーム・問題画面では三本線＝設定、それ以外では `‹`＝戻る）。`#app` 直下に `#navback`（左上・`ICON_BACK`）を新設して役割を分け、`#gear` は「設定を開く」に固定。`go()` は innerHTML の差し替えをやめ、`.hide` の**出し分けだけ**を行う（`isSheet` で判定）。`$("gear").onclick` の三項分岐も `go("settings")` だけに減った。出し分けは ホーム＝左なし/右に三本線、問題画面＝左に `#back`（ホーム）/右に `#menu`、設定・統計・遊び方＝左に戻る/**右は空**（その画面自体が設定なので開く意味がない）。②**画面ごとの CSS `#app:has(> #play:not(.hide)) #gear{display:none}` を廃止**し、出し分けを `go()` の 1 か所に寄せた。出す画面が画面ごとに違うので、`:has` を足していく形は追いにくいため。③**枠を外した。** 4.5 で絵柄は三本線に揃えたが**枠の有無がまだ違って**いて、ホームの `#gear` は `button` の既定（`1px solid var(--edge)`＋`background:var(--panel)`＋角丸 11px）が効いた**面のあるボタン**、問題画面の `#menu` は `.plainbtn` で線だけ、という食い違いだった（指示にあった `.iconbtn` ではなく、`#gear` は無 class で既定の `button` を拾っていた）。`#gear` と `#navback` に `.plainbtn` を付け、`#gear` の CSS は**位置だけ**（`position:absolute`/`top:12px`/`right:12px`/`z-index:50`）に減らした。見た目の指定は `.plainbtn` の 1 か所に集約。④**タップ領域は縮まず広がった。** `#gear` は独自の `--gearW`（42/38px）で `#menu` の `--btnW`（52/44px）より小さかった。`.plainbtn` に寄せたので **42→52 / 38→44**。参照 0 件になった `--gearW` と `TOKEN_METRICS` の `gear`（両モード）を削除した。⑤`top` を 10px → **12px** に変更。問題画面の `.top` の中の `#back`/`#menu` が y=12 なので、ホーム・問題画面・シートで右上の高さが揃う。⑥**シートの見出しが左上のボタンと重なる問題**を、`.sheet h2` に `padding-left:var(--btnW)`・`min-height:var(--btnW)`・`display:flex;align-items:center` を与えて解決（`margin:2px 0 12px` → `0 0 8px`）。ボタンは `#app` 直下に 1 つしか置かない方針なので、見出し側を寄せる形にした。実測で見出しの文字はボタンの右端ちょうど（56 / 64px）から始まり、**中心も 34.0/34.0・38.0/38.0 で一致**する。⑦検証（360×690 と 430×900、JS エラー 0）: ナビの配置と見た目 **33/33**（5 画面の左上・右上が仕様どおり、問題画面で `#navback`/`#gear` が出ず `#back`/`#menu` と衝突しない、4 つのボタンが全部 `--btnW` と同寸、ホームの三本線に枠も地も無く `#menu` と枠・地・透明度・絵柄が一致、`‹` の path が `CHEV_L`、title/aria が「戻る」「設定」、見出し・タイトル・`.info` と重ならない、スクロールなし）。巡回と状態保持 **10/10**（ホーム→設定→遊び方→戻る→統計→戻る→戻る→ホーム、問題画面→設定→遊び方→戻る→戻る→問題画面で**問題・式・手数・問題番号がすべて保たれる**、`#back` でホーム、`goBack()` の各段）。回帰はヒント挙動 41/41・ヒント行レイアウト 17/17・保留削除と報酬 29/29・`#alt` と `G.ci` 13/13。⑧**検証環境の訂正**: iframe を使う測定ハーネスは、iframe が最終の高さになる前に中の `applyMetrics()` が走って **compact に latch し、その後 resize が届かない**ため、4.8〜5.0 で「normal」と書いた測定の一部は実際には compact の値だった（`--gearW` が 430×900 でも 38px のままだったことで発覚）。プローブ冒頭で `applyMetrics()` を呼ぶよう直し、**5.1 の数値と、上に挙げた回帰はすべて本当の normal / compact で取り直した**。アプリ側の不具合ではない。GAME-SPEC **5-0 を新設**（左上・右上の役割と画面ごとの出し分けの表）。DESIGN.md 3-1-2 に枠を外したこと・`--btnW` への一本化・`--gearW` 削除を追記し、**`.iconbtn` と `.plainbtn` の使い分けの表を新設**、7 章のタップ領域を更新 |
| 5.2 | 2026-09-13 | **BLOB を組む処理を `make10.py blob` に実装し、3 区分を組み直した。** ①これまで BLOB は手元で組んで貼っていたが、`blob` サブコマンドが `docs/index.html` の `const BLOB=` の中身を差し替える形にした（`select_course()` ほか、試作スクリプトの内容を すべて make10.py へ移した。**リポジトリ外の試作には依存していない**）。 ②3 区分: **CHAL 1,929 問**（`min_score >= 17` の全部）/ **COURSE 1,000 問**（選抜規則）/ **FREE 18,797 問**（残り全部）。合計 21,726 問で、`puzzles` 全行を重複なく覆う （5.1 は 2,100 / 1,000 / 18,138 = 21,238 問）。`n` は `survivor_count`。 ③COURSE の選抜は決定的（乱数を使わず `COURSE_SEED="make10-course-v1"` と位置と問題から ハッシュで順位を付ける）。1,000 問すべて難易度の許容幅の拡張 0 回で埋まった。 ④★の導入位置に「その演算子が解答例で実際に効いている」条件を足した ―― **41問目が `8117` の `8 + 1 + 1 ^ 7` になっていた**（`^` が結果に効いていない）。6-1 は これを捕まえているが、`8117` は解が 2 本ともそれに該当するので 6-4 の救済で解答例に残る。 さらに `9 - ( 8 - 9 ) ^ 3` は 6-1 を通る（指数 3 を 0 にすると `(-1)^0 = 1` で値が変わるため 桁単位では「寄与している」と出る）。判定は 6-1 と同じ「値が変わるか」を**ノード単位**に 当て、**値が底と違う `^` ノードが 1 つ以上あること**を要求する。在庫は 204 → **51 問** （難易度7:4 / 8:20 / 9:5）で目標難易度 7・8・9 は据え置き。41問目は `2417` の `2 ^ 4 + 1 - 7` になった。⑤★階乗必須にも「階乗の引数 >= 3」を足した ―― 28問目が `0636` の `0! + 6 - 3 + 6` で、`0!` は値を潰してはいないが階乗が何をする演算かを 示していない。`1!`/`2!` は生成時に枝刈り済みなので引数は 0 か 3 以上しかなく、条件は 「>= 3」と書ける。在庫 1,835 → **1,112 問**（難易度8〜10 に 542 問）。28問目は `4144` の `4 + 1 * 4! / 4` になった。**変わったのは 41 問目と 28 問目の 2 か所だけで、他の 998 問と 統計は 1 つも動いていない**（両方を走らせて diff で確認）。⑥検証 8 項目を`blob` と **同じ実行**に入れ、1 つでも落ちたら書き込まない（件数と分割／難易度の境目／段の条件／ 重複と間隔／DB との一致／解答例の再評価（`build_evaluator` とは別実装の `_plain_eval`）／ ★の導入位置／埋め込みの安全性）。全 8 項目 OK。⑦DATA-SPEC 6 章が 5.2 の実装とずれて いたので直した ―― コードが参照する **6-5 / 6-6 が文書に無かった**。6-1 を一般判定に、 6-2 を「廃止」に、6-3 に「`is_redundant` を立てない」を、6-4 に `_rescue_sort_key` を、 6-5 / 6-6 を新設。6-B の採用条件から「最もラクな解が冗長解でないこと」を外した（6-5 に 置き換わっている）。⑧**残った不具合（未修正・要判断）: `G.swap` に 5.2 で消えた問題の コードが残っていると、本編のその位置が「（この試作には入っていない問題です）」で固定され、 「べつの問題」でも抜け出せない**（`findAlt()` がプレースホルダの `nf`/`np` が `undefined` の ため候補 0 件になる）。ただし `#alt` は `HIDDEN_UI` で隠れているので、現行の試作では `G.swap` に値が入る経路が無い。GAME-SPEC 6-1（件数とモード別内訳）・6-2（難易度の範囲 3〜45 → 3〜44）・7-1（★の条件と `OP_COST` 依存の警告）、DATA-SPEC 5 章（`blob`）・ **8-B を新設**・6 章全面改稿・6-B を更新 |
| 5.2 追補 | 2026-09-16 | **5.2 の変更履歴の訂正**（`index.html` は無変更・**APP_VERSION は 5.2 のまま**）。5.2 の行に書いた「**変わったのは 41 問目と 28 問目の 2 か所だけで、他の 998 問と統計は 1 つも動いていない**」は、**5.2 の作業中に 2 通りの `blob` を走らせて突き合わせた数字**（★の導入位置に条件を足す前と後）であって、**コミット済みの 5.1 の BLOB と比べた数字ではない。** 過去の行は当時の記録として 残すので、訂正はこの行に書く。5.1（HEAD）の BLOB と作業ツリーの BLOB を実際に 突き合わせた結果は次のとおり ―― **COURSE は 1,000 問すべてが入れ替わっており、同じ位置で一致するものは 0 問。** 旧 COURSE 1,000 / FREE 18,138 / CHAL 2,100 ＝ 計 21,238 問・723,504 文字、新 COURSE 1,000 / FREE 18,797 / CHAL 1,929 ＝ 計 21,726 問・733,118 文字。`(id,rc)` で見て消えた 2,511 問・増えた 2,999 問・共通 18,727 問、共通のうち解答例が変わった 5,374 問・難易度 `d` が変わった 5,278 問・`n` が変わった 12,246 問・区分が変わった 2,255 問。`index.html` 全体は 829,243 文字で、**その 88.4% が BLOB**。COURSE が総入れ替えになったのは、5.2 で選抜規則そのものを `select_course()` に作り直したためで、想定どおりの結果である |
| 5.3 | 2026-09-16 | **5.2 の積み残し 4 件。BLOB と DB は作り直していない。** ①**存在しない問題コードで本編が詰む不具合を修正。** 原因を特定してから直した ―― `nextOf("course")` が `G.swap[G.ci]` を無条件に `byCode()` へ渡し、`byCode()` は BLOB に無いコードでも必ずプレースホルダ（`d:0` / `n:0` / `nf`・`np` が `undefined` / `sol` が「（この試作には入っていない問題です）」）を返していた。**これは 10 を作れないので `G.ci` が永久に進まず、本編がその位置で詰む。** `#alt`（べつの問題）は `HIDDEN_UI.alt=true` で非表示、仮に押しても `findAlt()` が `q.nf===p.nf` を要求し `undefined` に一致する問題が無いので候補 0 件・`G.swap` は書き換わらない。「全部消す」もホームに戻って入り直しても同じ。しかも段階 3 のヒントを開くと プレースホルダを解答例として出したうえで**残数を実際に 3 消費する**。**JS エラーは 0 件なので黙って詰む。** 直し方は `byCode()` の既定を「BLOB に無ければ `null`」に変更。呼ぶ側 2 か所（共有コード・統計の履歴のクリック）は元から `if(!p)` で守られていたので、そのまま正しい挙動になった。表示専用の `byCodeShown()` を 分け、統計の履歴の行だけは消えた問題でも残す（4 桁と制約はコードから復元できる）。②**読み込み時の選別。** `load()` で `swap` / `chalDone` / `chalShown` から今の BLOB に 無いコードを捨てる。**`best` / `seen` / `hist` / `hints` は捨てない** ―― 自己ベストと 「解いた事実」は記録そのもので、BLOB の組み替えで消してよいものではない。捨てるのは**読み込んだ後のメモリ上だけで、保存 JSON は書き換えない**（試作中は BLOB を 何度も組み替えるので、次の版で問題が戻れば進捗も生き返る。ここで `save()` すると戻せない）。`nextOf()` 側にも既定へ戻す二重の守りを入れた。あわせて `ALL()` を毎回 21,726 要素 作り直さないよう `INDEX`（`Map`）を新設し、`byCode()` の線形探索をやめた。③**`blob` の検証を 8 項目 → 12 項目に。** 9. 難易度カーブ（100 問ブロックの平均が 単調非減少）／10. 後半の難易度（201 問目以降が全部 `d` 9〜16）／11. 難易度の範囲（全区分が `d` 3〜44。区分の境目を見る 2 とは別に上下の端を見る）／12. **再現性**（同じ DB・同じ `COURSE_SEED` でもう一度組んでバイト単位で一致）を追加。12 のために組み立てを `_blob_build()` に切り出し、2 回呼んで突き合わせる。**実装側にしかなかった 4 項目（DB との一致・解答例の再評価・★の導入位置・埋め込みの安全性）はそのまま残した。** 定数 `DIFF_MIN/MAX` `COURSE_BLOCK` `COURSE_LATE_FROM/RANGE` を新設。④**検証（すべて実測）**: `blob --dry-run` で **12/12 OK**（再現性は 2 回組んで 733,121 バイトが一致、100 問ブロックの平均は 7.96 / 9.88 / 10.45 / 10.45 / 10.84 / 11.2 / 12.05 / 12.83 / 13.05 / 13.05、201 問目以降 800 問が実測 9〜16、全 21,726 行が `d` 3〜44）。不具合の修正は **22/22**（消えたコードを `swap` に仕込むと捨てられて本編の既定の問題に 戻る／生きている差し替えは残って効く／`chalDone`・`chalShown` が捨てられ「0 / 1929」に 戻る／`best`・`seen`・`hints`・`hist` は残り累計スコアも保たれる／履歴の行は残るが 押しても開かない／消えたコードの共有入力は「コードが正しくありません」／`G.ci` が 範囲外でも最後の問題で頭打ち）。動作検証は **53/53 を normal 430×900 と compact 360×690 の両方**で（3 モードの出題／本編のカーブ＝1〜4 問目が `d`3・11〜20 問目が全部括弧入り・26〜28 が階乗必須・41〜43 が累乗必須・1〜50 が制約なし／ヒント 1「使う記号」2「形」3 が今の解答例で 3 消費／「正解 N 通り」が今の `n` と一致／共有コードの生き・死に・でたらめ／件数が `CHAL.length`＝1,929 と `COURSE.length`＝1,000／挑戦 80 回連続で重複 0・残り＝全問−出題済み／挑戦 2 問クリアでヒント 1 個とトースト／進捗 25.0% と統計に `NaN` なし／スクロールなし）。**JS エラーは全工程で 0 件。** ⑤**測って分かったが直していないこと**: 挑戦モードの報酬は `win()`（正解した瞬間）で 数えるのに、クリア済み `chalDone` は `advance()`（「つぎへ」）で積む。正解して 「つぎへ」を押さずにホームへ戻ると、**ヒントは配られるのにホーム・統計の 「N / 1,929」は増えない。** 5.0 が報酬を意図して `win()` に置いた結果の食い違いで、5.3 で持ち込んだものではない。直すと表示の意味が変わるので**報告のみ**とする。DATA-SPEC 8-B の「blob の検証（8 項目）」を 12 項目に改め 9〜12 を追記、GAME-SPEC **9-1 を新設**（今の問題データに無いコードの扱いの表と、捨てる／残すの理由）|
| 5.4 | 2026-09-18 | **クリアの確定を `win()` に集約し、検証ハーネスを常設化した。** ①**不具合**: 報酬（5.0）は `win()`（正解した瞬間）で数えるのに、`chalDone` / `G.seen` / `G.cleared` は `advance()`（「つぎへ」）が担っていた。**正解して「つぎへ」を押さずホームへ戻ると、ヒントは配られるのにクリア数が増えない。** しかも挑戦の問題は表示した時点で `chalShown` に積まれ二度と出題されない（4.5）ので、**その分は「N / 1929」では永久に埋まらない**。実測でも 3 問続けて解いてホームへ戻ると `chalDone=0` / `seen=0` / `cleared` 据え置きのままヒントだけ +1 になった。②**直し方（案 A）**: `chalDone` の push・`seen` の更新と `firstClear` の判定・`cleared` の更新を `advance()` から `win()` へ移した。`firstClear` は `seen` を積む**前に**取る（順序を崩すと `cleared` が増えない）。`advance()` に残したのは進行だけ（`MODE==="course"` の `G.ci++` と `nextOf()`・`loadPuzzle()`）。二重計上は `solved` のガードが従来どおり防ぐ（「つぎへ」の 2 回目以降も `solved` で無視）。③**副作用（意図して受け入れた 2 つ）**: `G.cleared` は挑戦の解放条件（`CHAL_AT=100`）なので、**つぎへを押さなくてもロックが解ける**。これは「クリアはクリア」で意図どおり。もう 1 つ、**共有コードで解いた問題もクリアとして数えるようになった** ―― `advance()` は `MODE==="share"` では元々呼ばれないので、移したことで数え方が変わった側。記録（`best`/`hist`/`stats`）は 5.3 までも共有で増えていたので、`seen`/`cleared` だけ数えないほうがちぐはぐだと判断した。④**常設ハーネス**: `verify_ui.py` ＋ `uiharness/`（`cdp.py` / `ui.py` / `cases/*.py`）を新設。headless Chrome を CDP で動かす。**標準ライブラリだけ**で npm に依存しない（環境に Node が無く、入れると検証がまた使い捨てになるため）。ケースは 5 本 ―― `boot`（起動と画面の巡回）/ `save_migration`（保存データの移行）/ `modes`（3 モードの出題と件数）/ `hints`（ヒントの消費規則）/ `reward`（クリアの確定と挑戦の報酬）。**入れるのは回帰だけで、寸法の詰めや縮小率の実測のような計測ものは入れない。** 作り直すたびに踏んだ 4 点を実装に入れた ―― Chrome のパスは `MAKE10_CHROME` で上書き／`file://` ではなく 127.0.0.1 の HTTP で開く（`localStorage` のため）／画面の大きさは `Emulation.setDeviceMetricsOverride` で**読み込みより先に**決める（5.1 の latch）／判定はできるだけ DOM の見える値で取る（4.5→4.6 の見落とし）。**1 つでも落ちたら赤**（終了コード 1）。⑤**検証**: `python verify_ui.py` が **334 / 334 項目**（normal 430×900 と compact 360×690 の両方、各 167 項目・26.7 秒・終了コード 0・JS エラー 0）。内訳は boot 31 / hints 31 / modes 19 / reward 50 / save_migration 36（×2）。修正そのものは、挑戦を「正解 → つぎへを押さずホーム」で 3 問続けて回し、`chalDone` / `seen` / `cleared` / ホームの「N / 1929」がそろって増え、2 問目でヒント +1 とトーストが出ること、「つぎへ」を押しても二重に増えないこと、本編は `ci` だけがつぎへで進むこと、フリーでは報酬が増えないこと、解き直しで二重に数えないこと、`cleared=99` から 1 問解くと**つぎへを押さずにロックが解けて挑戦に入れる**ことを実測。**ハーネスが本当に赤くなることも確認した** ―― 同じ `reward` ケースを 5.3 の `index.html` に当てると **50 項目中 21 項目が落ちる**（5.4 では 0）。⑥**BLOB と DB は触っていない。** GAME-SPEC 3-1 を全面改稿（確定するものと「つぎへ」が行うことを表に分け、5.3 までのずれと何が埋まらなかったかを記録）、7-2 に `cleared` が正解の瞬間に増えること・それが意図どおりであること・モードを問わないことを追記。CLAUDE.md にファイル構成 2 行と「画面の検証（常設ハーネス）」節を追加 |
| 5.4 追補 | 2026-09-18 | **`AGENTS.md` の再発の記録と、`test_make10` の修正**（index.html は無変更・**APP_VERSION は 5.4 で据え置き**）。①**`AGENTS.md` がまた作られていた**（2026-09-18 13:58:18）。中身は今の CLAUDE.md（216 行）の "CLAUDE.md" を "AGENTS.md" に置換した複製で、4.7 追補と同じ **Codex デスクトップアプリの外部エージェント設定の取り込み**（`~/.codex/state_5.sqlite` の `external_agent_config_imports`、完了 13:58:18.605、`provider_id = "claude-code"`）。**削除を指示された時点で既に無かった**（`find` と `git status` で確認）ので、この作業では何も消していない。`.gitignore` には入れない（見えなくすると次に気づけない。これまでの方針どおり）。②**回数は「4 回目」ではなく、Codex の記録では 8 回目。** `AGENTS_MD` の取り込みは 9/8 22:47:33・9/9 11:04:55・9/10 04:17:24・9/10 16:48:33・9/11 13:43:39・9/12 04:16:53・9/13 07:03:37・9/18 13:58:18 の 8 回（9/13 19:03:38 の取り込みでは作られていない ―― 既に在ったため）。この変更履歴に残っているのは 4.7 追補の 1〜2 回目だけで、3〜7 回目は記録が無い。③**セッション記録の取り込みも今回が初めてではない。** 同じ取り込みで、このリポジトリの Claude Code のセッション記録（`~/.claude/projects/C------make10/*.jsonl`）が Codex へ取り込まれていた ―― 9/8 に 7 本、9/10 に 1 本ずつ 2 回、9/12 に 1 本、9/13 に 2 本、今回 4 本（この作業のセッションを含む）の**計 16 本**。4.7 追補の調査では `AGENTS_MD` だけを見ていて、同じ行の `SESSIONS` を報告していなかった。④**`test_make10` が 50 本中 4 本落ちていた**（5.2 で make10.py を変えたときにテストを直していなかった。HEAD の make10.py では通る）。**テストを緩めずに直した。** (a) `classify_nullified()` は 5.2 で「パターンの列挙」から「その桁を別の値に替えても値が変わらないか」という一般判定に変わり、引数も `(tree, val)` → `(tree, digits, base)` になった ―― 一般判定とパターン判定（`_nullified_by_pattern`）を両方とも確かめる形にした。`test_nullified_needs_operator_in_x`（`5 ^ 0` の葉は対象外）は 5.2 で葉にも広げたので**期待が逆になる** ―― `test_nullified_leaf_base_is_caught` に改め、代わりに「どの桁も効いている式は掛からない」と「差し替えられないだけで効いている桁（3343 の `( 3! )!`）は掛からない」の 2 本を足した。(b) `Curate64OnlySolution` は問題 0050 を使っていたが、6-2 の廃止で 0050 は 6 解が残り救済が起きなくなった ―― **救済の経路は、今の 6-1 で実際に全滅する 0075 で確かめる**形にし（6 解中 5 解が 6-1・1 解を救済、残る解は `make10.db` と同じ `( 0! + ( 0 * 7 )! ) * 5`）、0050 は「救済が不要になった」回帰テストとして残した。(c) `Constrain` の冪等性は、`promoted_repr` が「**この実行で `is_repr` を 0 → 1 に昇格させた解答例の数**」（6-6）で、昇格が DB に残るので 2 回目は 0 になるのが正しい。**本物の DB の複製でも確かめた** ―― `constrain` を 2 回流しても、`curate → constrain` で流し直しても、`puzzles` / `solutions` は元の `make10.db` とハッシュで完全一致し、他の統計 8 項目も一致（`promoted_repr` は 0 / 0 / 3,079）。テストは「DB の中身が一致」「2 回目の昇格は 0」「それ以外の統計は一致」「正規の順で流し直すと統計も DB も 1 回目と完全一致」に直した（前は統計の比較だけで、DB の中身は見ていなかった）。**53 / 53 で通る**（50 本 → 53 本）。⑤**直していないこと**: DATA-SPEC 6-1 と `classify_nullified()` の docstring にある「`0,0,0,9` のような問題は解が全滅する」は実データと合わない ―― 0009 は 562 解のうち 189 解が冗長でなく、救済されていない |
| 5.4 追補 2 | 2026-09-18 | **締めの整理**（index.html は無変更・**APP_VERSION は 5.4 で据え置き**）。①バックアップ DB の整理: `make10_backup_pre52` / `pre52b` / `pre52c` / `pre52d` と `make10_backup_full.db`（古い版の generate）を削除し、`pre52e` を復元用に残した（ファイル構成の表に用途を記載）。`make10_backup_0099.db` は削除の指示が無いので残し、判断を報告した。②`.gitignore` を `make10_backup_*.db` と `make10*.db-shm` / `make10*.db-wal` の形に（個別のファイル名を並べない）。③DATA-SPEC 6-1 と `classify_nullified()` の docstring にあった「`0,0,0,9` のような問題は解が全滅する」を訂正（5.4 追補 ⑤で報告した件）。`0009` は `0!` で 1 を作れるので全滅しない（`make10.db` の実測で 562 解中 189 解が非冗長）。全滅の実例は `make10.db` の 6-4 救済 512 問から `0075` に差し替えた（6 解すべてが 7 を潰し、`( 0! + ( 0 * 7 )! ) * 5` が救済される） |
| 5.4 追補 3 | 2026-09-19 | **検証ハーネスが古い版を読む問題の修正と、件数・選び方の記述の全数点検**（index.html は無変更・**APP_VERSION は 5.4 で据え置き**）。①**原因を再現してから直した。** 一時ディレクトリに 5.3 の `docs/`（更新時刻＝今）を置いて 1 回読ませ、次に 5.4 の `docs/`（更新時刻＝2026-01-01）を同じポート・同じプロファイルから配ると、Chrome が `If-Modified-Since` を送り、標準の `SimpleHTTPRequestHandler.send_head()` が（応答ヘッダを書く前に）304 を返して、**5.4 を配っているのに 5.3 が動いた**。Python 3.14.7 のソースで 304 の分岐を確認。②**3 層で塞いだ**: 配信（`cdp._QuietHandler` が条件付き要求ヘッダを消してから標準処理に渡し、`Cache-Control: no-store` を付ける）／ブラウザ（`Network.enable` → `Network.setCacheDisabled(true)`。CDP セッションは 1 本で、ケースごとにタブを作り直していない）／プロファイル（`tempfile.mkdtemp()` で実行ごとに作り終了時に消す。どのケースも `ui.open()` が毎回 `localStorage` を消してから仕込むので、前回の実行の `localStorage` には依存していないことを確認済み）。同じ再現手順で 3 層それぞれ単独でも 5.4 が読まれることを確認（全部外すと 5.3）。③**版の照合**: `verify_ui.py` の起動直後に、ソースの `APP_VERSION` と設定画面に見えている `#appver` を突き合わせ、食い違えばケースを回さず終了コード 1。5.3 を配って赤、表示行だけ書き換えて赤を確認（`docs/` は元に戻し差分 0）。`verify_ui.py` の「つまずいた点」に 5 番目、本ファイル「画面の検証」節に 2 項目を追加。④**`curate()` の docstring の誤り**（救済は「実測で 0 件」→ `make10.db` で 512 問、残す 1 件は「6-3 の読みやすさ順」→ `_rescue_sort_key` のスコア最小 → 読みやすさ → id）を直し、`make10.py` 全体と `DATA-SPEC.md` 全体の件数・選び方の記述を grep で全数点検した。**方針: `make10.py` には件数を書かず、DATA-SPEC 6-7（新設）と各節の実測値に数え方（SQL）付きで集める。** 設計判断のときの比較値は数字を残し「5.2 作業時点の実装・データでの値。今の DB では再現しない」と注記。直した主なもの: 第 6 章の見出しコメント（6-2 が現役扱い・救済の選び方）、constrain の見出しコメント（6-5 の救済を反映）、DATA-SPEC のスキーマのコメント 4 行、「候補 12 通り」→ 6 通り（3 箇所）、「解答例は `is_repr = 1` から選ぶ」→ `_example_key`、6988 の解 8 → 10、難易度 9 以下の階乗必須 676 → 646、制約付きが作れない 1,798 問の内訳（「解が 1 つしかない 595 問」→ 343 問、残りの説明も実データに合わせた）、`problems.min_score` の母集団 |
| 5.5 | 2026-09-20 | **難易度を「一番簡単な解き方」で決め、一番簡単な解き方が数字を潰す式になる問題を出さなくした。** ①**理由**: 5.4 までは難易度を「6-1（数字を潰す式）を除いた解の最小スコア」で付けていたが、プレイヤーは `7 + 2 * 0 + 3` のような 6-1 の式も入力できて正解になる。5.4 の DB で 2,753 件の puzzle が実際より難しく表示されていた（挑戦モードだけで 729 件）。②**規則**（`constrain`）: 規則 1 ＝ `min_score` は制約を満たす全解（6-1 を含む）の最小、`base_min_score` は全解の最小、`harder_by` はその差。規則 2 ＝ 6-1 の解（`redundant_why` が `6-1`/`6-4` で始まる解。`_is_nullified`）の最小が 6-1 でない解の最小**以下**なら採らない（同点も採らない。無制約・制約付きの両方）。規則 3 ＝ 解答例は今までどおり冗長でない解の `_example_key` 最小。規則 2 を通った puzzle では一番簡単な解が 6-1 でない解なので、解答例のスコアと難易度が必ず一致する ―― `constrain` は一致しなければ `RuntimeError` で止まる。`curate` の `problems.min_score`/`max_score` も全解に。export の `difficulty` と `_blob_rows()` は `puzzles.min_score` を読むだけなので無変更。③**手順 1 の空実行**（実装前・複製の DB で）: 残るのは無制約 7,270・制約付き 10,521、`select_course()` の許容幅の拡張 0 回、★の在庫は 26〜28 が 200/181/137 問・41〜43 が 4/20/5 問（5.4 と同じ）で止まる条件なし。6-4・6-5 の救済は、規則 2 が「6-1 でない解が無い puzzle」を先に落とすので `puzzles`・BLOB・export に影響しなくなった（6-5 の分岐は通らず `rescued_puzzle`=0、6-4 は `problems` の `repr_solution_id`/`solution_count` と印にだけ残る）。救済の処理は**消していない**。④**結果**: 17,791 問（COURSE 1,000 / FREE 15,588 / CHAL 1,203）。5.4 の 21,726 問（1,000 / 18,797 / 1,929）から 4,627 問が消え（COURSE 326・FREE 3,517・CHAL 784）、692 問が加わった（59・575・58）。両方にある問題の難易度は 1 つも変わっていない。COURSE の選抜規則とカーブの定数は変えておらず、100 問ブロックの平均は 5.4 と同じ（7.96 … 13.05）。同じ位置に同じ問題が残ったのは 614/1000。5.4 の COURSE 1〜100 問目のうち 21 問が除外された。⑤**検証 13 を追加**（`blob`）: BLOB の全行について `solutions` 表から SQL で直接「制約を満たす 6-1 の解の最小」と「6-1 でない解の最小」を求め、前者が真に大きいことと `d` が後者と一致することを見る。`constrain` の関数は呼ばない。**最初の実装は `NOT (NULL LIKE …)` が NULL になって冗長でない解を数えられず 11,296 行が赤になった**（検証側の誤り。`COALESCE` で直した）。規則 2 と `constrain` の保護をわざと外して流すと 13 が 4,775 行を捕まえて赤になり書き込みを止めること、規則 2 だけ外すと `constrain` の保護が `0009` で止めることを確かめ、元に戻した。⑥**テスト**: `Constrain.test_invariants_and_idempotent` の「各問題に無制約の puzzle がちょうど 1 件」を「規則 2 を満たす問題にだけ 1 件（SQL で独立に判定）」に、制約付きの残存解の比較相手を無制約 puzzle から `solutions` の件数に、`base_min_score` を全解の最小に直し、全 puzzle で規則 1・2 を SQL で確かめる検査と「無制約の puzzle が無い問題が 1 つ以上ある」（空振り防止）を足した。53/53。⑦**verify_ui**: `modes` の件数を 1,203 / 15,588 / 17,791 に、挑戦の「N / 全問」は `CHAL.length` から組むように（`modes`・`reward`・`save_migration`）、CHAL が全部 d>=17・COURSE/FREE が全部 d<=16 を追加。338/338。今回消えたコードを `chalDone`/`chalShown`/`swap` に仕込んだ保存データで、9-1 どおりに捨てられて「1 / 1203」になることを一時スクリプトで確認（9/9）。⑧**バックアップ**: 作業前の `make10.db` を `make10_backup_pre55.db` に複製（ハッシュ一致）。DATA-SPEC 4 章・6-1・6-3・6-4・6-5・6-7・6-B・6-D・7 章・8 章・8-B、GAME-SPEC 6-1・6-2・7 章・7-1・9-1 を更新。⑨**追記（コミット前）: 本編の段の条件の書き漏れ。** 11〜25 問目の解答例に `!` や `^`、29〜40 問目に `^` が出ていた（20 問目 4179 `4 + ( 1 - 7 + 9 )!`・22 問目 3983・23 問目 2471 `2 ^ 4 - 7 + 1`・24 問目 7459・25 問目 9596・35 問目 2527）。GAME-SPEC 7-1 は階乗を 26〜28、累乗を 41〜43 の★で初めて出す設計だが、`_course_stage_cond()` は 5.2 で 5〜10 問目にだけ `^` `!` の除外を書き、11〜25 と 29〜40 に書き漏れていた（5.4 でも 24 問目が `1 ^ 3 + 9 * 1`）。意図的に許していた記録は変更履歴・GAME-SPEC・DATA-SPEC・コードのコメントのどこにも無いことを確かめてから直した。1〜25 問目は `^` `!` なし、29〜40 問目は `^` なしを条件に足した。許容幅の拡張は 1,000 問すべて 0 回のまま、100 問ブロックの平均も不変。1〜50 問目で入れ替わったのは 20・22・23・24・25・35 問目で、5.4 と同じ位置に同じ問題が残るのは上の 614 から 611 になった。`blob` に**検証 14**（COURSE の解答例の文字列を直接見て、1〜25 問目に `^` `!`、29〜40 問目に `^` が無いこと。`_course_stage_cond` は使わない）を足し、段の条件を元に戻すと 5 問＋1 問を捕まえて赤になることを確かめた。GAME-SPEC 7-1 の段の表を実装の条件どおりに、DATA-SPEC 8-B に段の条件の表と検証 14 を追加。⑩**追記 2**: 26〜28 問目（★階乗）にも `^` なしを足した（累乗は 41〜43 の★で初めて出すため。⑨で漏れていた）。検証 14 の範囲を「1〜25 問目の `!`・1〜40 問目の `^`」に広げた。今の 26〜28 問目に `^` は元から無いので BLOB は 1 バイトも変わらず（`cmp` で一致）、除外を外しても赤にならない ―― 判定部分に `^` を含む解答例を 26・27・40 問目に与えると赤、41 問目の `^` と 26 問目の `!` は通すことを一時スクリプトで確かめた |
| 5.6 | 2026-09-23 | **難易度に「10 の倍数を作って割り戻す」加点（10a/a）を入れた。** ①**規則**: 二項演算 `a / b` で 値(a) == 10 × 値(b) かつ 値(b) >= 2 のノードがあれば **+2**（`SCORE_BONUS["ten_over"]`）。まっすぐ 10 を作らず `5 * 6 / ( 6 - 3 )` のように 30 を作ってから割り戻す遠回りに手応えがあるため。判定は `is_ten_over()` の 1 か所だけに持ち `score_solution` から呼ぶ。**式の途中のノードでもよい**（`0 + 2 * 5! / 4!` の 240/24）。除外は 3 つ ―― 割る数が整数でないもの（`4! / ( 0! + 7 / 5 )` は 24 ÷ 2.4 で 10 の倍数を作っていない。76 ノード）／`a` と `b` がどちらも階乗ノード（`( 0! + 0! + 8 )! / 9!` は n!/(n-1)! の手筋で別物。2,119 ノード。`5! / ( 7 * 2 - 2 )` のように割る側が階乗でないものは対象のまま）／掛けた分をそのまま割り戻す打ち消し（`( 8 + 2 ) * 2 / 2`。748 ノード。`a` を `*` `/` で展開した掛ける側の項に 値(b) と同じ値があるか。展開は `*` `/` のノードだけを降り、括弧の中の足し引きは 1 項として値だけ見る）。**加点は 1 解につき 1 回**（対象が 2 つあっても +2）。②**手順 1（実装前の空実行。複製の DB で測り、止まる条件 3 つに当たらないことを確かめてから実装）**: 対象は 11,925 解 / 246,977 解（対象ノードも 11,925 ―― 狭めた後は 2 ノードの解が 1 つも無い。前回の一律案は 14,120 ノード / 14,040 解）。`select_course()` の許容幅の拡張 0 回、★の在庫は 26〜28 が 200/181/118 問・41〜43 が 4/20/5 問（累乗側は 5.5 と同じ）、`d` の最大は 44 のまま（前回の一律案では `3799 +=0` が 46 になり `DIFF_MAX` を超えていた ―― 階乗の比を対象外にして解消）。加点 0 で組み直すと `make10.db` の `puzzles` 17,791 行と `min_score`/`base_min_score`/`harder_by`/`example_solution_id`/`survivor_count` まで完全一致することを先に確かめてある（手順の検算）。③**結果**: 17,793 問（COURSE 1,000 / FREE 15,478 / CHAL 1,315）。5.5 の 17,791 問（1,000 / 15,588 / 1,203）から 130 問が消えて 132 問が増え、両方にある 17,661 問のうち `d` が変わったのが 1,509 問・解答例が変わったのが 336 問（`_example_key` は変えていない。加点後のスコアで選び直された）。区分の移動は FREE→CHAL 101・COURSE→CHAL 13・FREE→COURSE 229・COURSE→FREE 224。問題の最小スコアが上がったのは 359 問 / 9,161（+1 が 92・+2 が 267）。**規則 2 で無制約の puzzle が 20 問消えた**（`8769` は最短が `( 8 + 7 ) * 6 / 9`（7 点）で、加点すると 6-1 の `( 8 - 7 ) ^ 6 + 9`（8 点）に抜かれる。ほかに `9888` `9999` など。これは 5.5 の規則 2「一番簡単な解き方が数字を潰す式になる問題は出さない」の帰結で、加点が 6-1 の式の相対的な安さを上げる副作用）。COURSE の 100 問ブロック平均は 5.5 と同じ（7.96 … 13.05）、同じ位置に同じ問題が残ったのは 702/1000（1〜50 問目では 39/50）。④**検証 15 を追加**（`blob`）: BLOB の全行の解答例を `parse_tree` で木に起こし、**独立実装**の `_plain_walk`/`_plain_ten_over`/`_plain_score` で「10a/a の有無」と「加点を除いた素の点」を組み直して `d` と突き合わせる（`score_solution` も `is_ten_over` も呼ばない。重みの表だけは仕様の値なので共有）。対象は 1,313 行 / 17,793 行。**赤くなることの確認**: `is_ten_over` から分数の除外を外して一巡させると 4 行、階乗の比の除外を外すと 183 行を捕まえて赤になり書き込みを止める（リポジトリ外の複製 DB と複製した `make10.py` で実行。`docs/` には書いていない）。「1 解 1 回」だけは今のデータでは赤にできない ―― 対象ノードが 2 つある解が 1 つも無いため。⑤**`verify_render_roundtrip.py` にも独立実装で加点を足した**（`ten_over()`。`make10.py` を参照しない作りは維持）。足さないと 11,925 行が `score` 不一致で赤になる。246,977 行で不一致 0。⑥**テスト**: `TenOverBonus` を 8 本追加（対象になる 3 形・式の途中・分数/階乗の比/打ち消し/`b=1` の 4 除外・スコアの式・人工の木で「対象 4 ノードでも +2」）。式はすべて `make10.db` に実在するものを使った。53 → **61 本**。⑦**verify_ui**: `modes` の件数を 1,315 / 15,478 / 17,793 に。338/338。5.6 で実際に消えた 130 件のコードを `swap`/`chalDone`/`chalShown`/`best`/`seen`/`hints`/`hist` に仕込んだ保存データが 9-1 どおりに扱われること（捨てる 4 つ・残す 4 つ・保存 JSON は書き換えない・「1 / 1315」・本編が既定の問題に戻る）を一時スクリプトで確認（17/17）。⑧**バックアップ**: 作業前の `make10.db` を `make10_backup_pre56.db` に複製（ハッシュ一致）。`annotate → curate → constrain → blob` を一巡。⑨DATA-SPEC 6-7・6-B・6-D・7 章（10a/a の節を新設）・8-B（区分の件数・検証 15）、GAME-SPEC 6-1・6-2・7 章・7-1 を更新。CLAUDE.md と `verify_ui.py` の「`blob` の 12 項目」を 15 項目に直した（5.3 以降ずれていた） |
| 5.7 | 2026-09-25 | **難易度に「隣り合う階乗の比」の規則を 3 つ入れ、`DIFF_MAX` を 45 に上げた。** ①**規則**（判定は `is_ten_over` と同じ場所にまとめ、`score_solution` から呼ぶ）: **つながり**＝`*` `/` だけで繋がったひとかたまり（`iter_conn()` が各ノードの「つながりの根」を返す）を `_mul_split()` で掛ける側・割る側の項に分け、**規則 1** ＝ 10a/a の候補ノードを含むつながりに `(m+1)!/m!` の組があれば 10a/a の加点を付けない（`( 0! + 0! ) * 5! / 4!` は 240/24 だが、やっているのは 5!/4! の比）／**規則 2** ＝ 解のどこかのつながりに組があれば **+2**（1 解 1 回。`SCORE_BONUS["fac_ratio"]`）／**規則 3** ＝ 木の根が `/`・値が 10・**根の左右が直接その階乗**なら さらに **−1**（`SCORE_BONUS["whole_ratio"]`。`( 0 + 1 + 9 )! / 9!` は 10 へ至る道のりが比ひとつしかないため）。組の照合は**階乗の引数**で、掛ける側は項の中まで降りるが **`+` `-` をまたぐときはもう一方の枝の値が 0 の場合だけ**降りる（`_ratio_num_facs`）。`( 6! / 2 - 5! ) / 4!` の `5!` は比ではなく 240 を作って割り戻す 10a/a、`0! + ( 0 + 9! ) / 8!` の飾りの 0 は今までどおり拾う。割る側は項そのものが階乗のときだけ拾う。②**測定（実装前・リポジトリ外の複製で）**: 3 規則を入れない状態で組み直すと `puzzles` 17,793 行・`solutions` 246,977 行・BLOB 595,191 文字が作業ツリーと完全一致することを先に確かめた。規則 1 で 10a/a が外れる **849 解**・規則 2 **8,420 解**・規則 3 **1,939 解**（規則 1 は必ず規則 2 を伴う）。**詰める前は「`+` `-` をまたぐ組」を拾っていて、比として読めない組が 694 解にあった**（`0! + ( 7! + 8! ) / 7!` が 27 点に上がる・`( 6! / 2 - 5! ) / 4!` の本物の 10a/a が消える）ので、上の詰めを入れてから実装した。③**結果**: **17,792 問**（COURSE 1,000 / FREE 15,342 / CHAL 1,450）。5.6 の 17,793 問（1,000 / 15,478 / 1,315）から 8 問が消えて 7 問が増え（どちらも制約つきのみ・無制約の増減 0）、両方にある 17,785 問のうち `d` が上がったのが 691 問（+1 が 228・+2 が 463）・**下がったものは 0**・解答例が変わったのが 46 問。区分の移動は 課程 → CHAL の 133 問だけ。5.5 の規則 2 の判定が変わったのは 3 件（`0198` の `*=0` `-=0` が採用 → 不採用、`3098` の `-=0` が不採用 → 採用）。COURSE は許容幅の拡張 0 回・★の在庫 200/181/118 と 4/20/5・100 問ブロック平均・**1〜50 問目が 5.6 と完全に同一**で、同じ位置に同じ問題が 904/1000。④**`DIFF_MAX` を 44 → 45。** `3799` の `+` 禁止 `( ( 3! )! * 7! / 9! )! / 9!`（10! を作って 9! で割り、それを階乗してまた 9! で割る）が 45 になる。**45 の puzzle はこの 1 件だけ**（44 も 1 件）。難易度の上限が表示や区分に使われていないことを確認済み ―― 区分は `CHAL_MIN_SCORE`（17）、`#pcode` は `p.d` をそのまま出す、得点は `parSec=30+d*4` / `parWaste=6+d*.5` / `pts=d*20*k` の連続式、星は `d>=14 / >=9`、統計の帯は `17+` が開いた区間で、どこにも上限の表も頭打ちも無い。⑤**検証 15 を階乗の比まで見るよう拡張**（独立実装に `_plain_conns` / `_plain_split` / `_plain_num_facs` / `_plain_fac_ratio` / `_plain_whole_ratio` を追加し、`d == 素の点 + ten_over + fac_ratio + whole_ratio` を突き合わせる）。**赤くなることの確認**: 独立実装の側の条件を 1 つずつ外すと 規則 1 で 34 行・規則 2 で 690 行・規則 3 で 199 行・`+` `-` の詰めで 14 行を捕まえて赤になり書き込みを止める（リポジトリ外の複製で実行。`docs/` には書いていない）。⑥**`verify_render_roundtrip.py` にも独立実装で 3 規則を足した**（`conns` / `split_mul` / `fac_args_num` / `fac_ratio` / `whole_ratio`。`make10.py` を参照しない作りは維持）。外すと 規則 1 で 849 行・規則 2 で 8,420 行・規則 3 で 1,939 行・詰めで 190 行が `score` 不一致になる。246,977 行で不一致 0。⑦**テスト**: `FacRatioBonus` を 9 本追加（隣り合う比・離れた比は対象外・規則 1 の抑止・**`+` `-` をまたぐ組は拾わない（`( 6! / 2 - 5! ) / 4!`）**・**飾りの 0 は拾う（`0! + ( 0 + 9! ) / 8!`）**・割る側は項そのもの・見送った形（`4! / ( 8! / 7! ) + 7`）・規則 3 のスコア・人工の木で「組 2 つでも +2」）。式はすべて `make10.db` に実在するものを使った。61 → **70 本**。⑧**verify_ui**: `modes` の件数を 1,450 / 15,342 / 17,792 に。**338/338**。5.7 で実際に消えた 8 件のコードを `swap`/`chalDone`/`chalShown`/`best`/`seen`/`hints`/`hist` に仕込んだ保存データが 9-1 どおりに扱われることを一時スクリプトで確認（14 項目 × 2 モード = 28/28）。⑨**見送ったこと**: `4! / ( 8! / 7! ) + 7` のように**割る側の中にある比**は、つながりの分解で `8!` が割る側・`7!` が掛ける側へ回るため拾えない（153 解・うち解答例 20 件・6-1 の印つき 32 件）。条件が増えるわりに動く行が少ないので入れなかった（DATA-SPEC 7 章に記録）。⑩**バックアップ**: 作業前の `make10.db` を `make10_backup_pre57.db` に複製（ハッシュ一致）。`annotate → curate → constrain → blob` を一巡。⑪DATA-SPEC 6-7・6-B（実測値・増減の説明）・6-D・7 章（階乗の比の節を新設）・8-B（3 区分の件数・検証 11 の範囲・検証 15）、GAME-SPEC 6-1・6-2・7 章・7-1 を更新 |
